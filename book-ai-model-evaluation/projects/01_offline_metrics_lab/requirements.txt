# 离线评估指标库 —— 依赖清单
# 本项目刻意"零重型依赖"：核心指标全部纯 numpy 手写，不用 sklearn。
# 只需以下三个包即可跑通测试与出图（版本为下限，越新越好）。

numpy>=1.24        # 数值计算与向量化，指标库的唯一核心依赖
matplotlib>=3.6    # run_demo 出 ROC/PR/校准/切片图（Agg 后端，无需显示器）
pytest>=7.0        # 单元测试框架，跑 `python -m pytest -q`

# 说明：
#   * 不需要 GPU、不需要联网、不需要任何 API key。
#   * torch 虽然本机装了 2.12 CPU，但本项目并不依赖它（指标不需要）。
#   * 中文字体依赖系统自带的 "Microsoft YaHei" / "SimHei"，无需 pip 安装。
