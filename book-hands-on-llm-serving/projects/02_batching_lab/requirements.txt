# 批处理策略对比实验 —— 依赖清单
# 本项目完全离线：不下载模型、不联网、不需要 GPU 或任何 API key。
# 只用到科学计算三件套；torch 并非必需（内核是纯 numpy），列出仅为与本书其它项目环境一致。

numpy>=1.24          # 工作负载生成（泊松/几何分布）、百分位、向量化统计
matplotlib>=3.7      # run_demo.py 出图（时间线 + 指标柱状图），用 Agg 后端
pytest>=7.0          # 运行 tests/ 下的正确性与「物理规律」测试

# 可选：本项目内核未直接 import torch，但若你想扩展成真机对拍可用
# torch>=2.0        # CPU 版即可
