# 目标检测指标实验室 —— 依赖（全部为纯 CPU、可离线安装）
# 本项目核心算法只用 numpy；matplotlib 仅演示出图用；pytest 跑测试。
# torch 并非必需（项目未 import），列出仅为与本书其他项目环境一致，可按需删除。

numpy>=1.24
matplotlib>=3.6
pytest>=7.0

# 可选（本项目不直接依赖，环境已装 2.12 CPU 版）：
# torch>=2.0
