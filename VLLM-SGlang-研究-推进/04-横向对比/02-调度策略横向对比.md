# 调度策略横向对比

> **本篇取证基准**：`vllm` @ `7ca49fbe`（2026-08-22）；其余引擎 sha 见 `_lab/out/repo_stats.json` 的 `ref` 字段，跨引擎引用一律加 `引擎名:` 前缀。
> **一句话**：同一件事，八个引擎给出八种取舍

## 0. 结论先行

- **"连续批处理"这个词掩盖了三个独立子问题，每家引擎在这三个问题上分别选边站**：① 等待队列按什么顺序出队（排序策略）；② 显存/槽位不够时牺牲谁（让位机制）；③ 这一步给谁分多少 token（预算模型）。八个引擎在这三条轴上组合出的答案几乎两两不同，但没有一家在全部三条轴上都是异类——这才是横向对比真正有意思的地方。
- **排序策略上，FCFS 是默认多数派，前缀感知排序是少数派特性，不是行业共识**：vLLM/LMDeploy/LightLLM/llama.cpp/TGI/Tokasaurus 默认或事实上都按到达顺序处理；SGLang 的 LPM 和 TensorRT-LLM 的 `enablePrefixAwareScheduling`（默认开，`tensorrt-llm:cpp/include/tensorrt_llm/batch_manager/capacityScheduler.h:102`）是仅有的两处"排序/准入决策直接读前缀缓存命中率"的地方，但做法完全不同——SGLang 重排整个等待队列，TRT-LLM 只用它做"这个请求现在跳过是否划算"的二元判断。
- **让位机制上，"要不要允许抢占"本身是一条分水岭**：vLLM、SGLang、LMDeploy、Tokasaurus 默认允许牺牲已在跑的请求腾资源（叫法各不相同：preempt / retract / evict / bump）；TensorRT-LLM 默认策略 `GUARANTEED_NO_EVICT`（`tensorrt-llm:cpp/include/tensorrt_llm/executor/types.h:228`）、TGI、LightLLM、llama.cpp 则默认或架构性地**不**允许——它们靠更保守的准入控制（worst-case 预留、`GUARANTEED_NO_EVICT`、固定 slot）从源头避免走到"资源不够只能牺牲人"这一步。
- **一个独立收敛的反直觉现象**：在允许抢占的四家引擎里，vLLM（`self.running.pop()`）、LMDeploy（`running.pop(-1)`）、Tokasaurus（`running_seqs.pop()`）都选择了"牺牲最后加入 running 的请求"（LIFO 倾向），LightLLM 的迭代耗尽式实现在效果上也偏向新请求——只有 SGLang 反过来选"已生成 token 数最少者"。四个互不知情的工程团队分别收敛到近似 LIFO，这不是巧合，`## 7` 会给出机制解释。
- **收束判据（本篇不产出实测数字，只讲机制因果）**：连续批处理相对静态批处理的全部收益，来自两件独立的事同时发生——**输出长度异质**（否则没有人会提前结束、腾出名额）和**在线到达**（否则准入/抢占分支永远不会被触发）。这条判据在 [[03-vLLM-调度器解剖]] `## 5` 决策 5 里已用源码证据展开；本篇跨引擎复核后发现，**这条判据对全部八家引擎都成立，是唯一一条不因实现差异而改变结论的规律**——同质离线批量场景下，本篇讨论的全部排序/让位/预算差异都会退化成无意义的开销，这也是为什么本篇拒绝比较任何吞吐/延迟数字：机制差异只在异质+在线的流量下才产生可观测后果，脱离流量形状谈"谁的调度器更好"没有意义。
- **防饥饿这件事上，名气和完成度不对应**：多数读者会假设"最懂前缀缓存"的 SGLang 也最懂怎么防止前缀不匹配的请求被饿死，本篇核实后发现恰好相反——SGLang 的 LPM 排序没有任何基于等待时长的老化机制，反倒是知名度低得多的 LMDeploy，默认就在长 prefill 重排序里带着一条"age credit"公式（docstring 原话"to avoid starvation"，`## 5` 决策 6）。
- **调度器的文件行数不能直接读成"调度逻辑的复杂度"**：`## 2.2` 用实测的 `wc -l` 数字拆穿了两个陷阱——llama.cpp 调度所在的文件有 5,475 行，但调度逻辑本身只占其中一小段（大头是 HTTP/多模态等其它服务功能）；Tokasaurus 任务书给出的"684 行"只是纯算法文件，真正的抢占逻辑在另一个 1,064 行的文件里。体量对比要先划清"这些行到底在干什么"，再谈厚薄。

**速查：任务要求的问题在哪一节回答**

| 问题 | 对应章节 |
|---|---|
| 主对照表（8 引擎 × 6 列） | `## 2` |
| FCFS vs LPM，LPM 会不会饿死请求 | `## 5` 决策 1 |
| preempt/retract/pause/bump/evict 具体差在哪 | `## 5` 决策 2、`## 3` 术语对照表 |
| prefill/decode 怎么二选一，chunked prefill 默认与块大小 | `## 4` |
| 固定 slot（llama.cpp）vs 动态调度（vLLM）分别适合什么工作点 | `## 4.3`、`## 5` 决策 5 |
| 连续批处理什么时候没收益 | `## 0` 最后一条、`## 6` |

**三轴定位表：八家引擎在"排序/让位/预算"三条轴上分别站在哪一端**（细节见对应小节，这里只给方位，不重复论证）：

| 引擎 | 排序轴（FCFS ←→ 前缀感知重排） | 让位轴（架构性不允许 ←→ 默认激进抢占） | 预算轴（静态共享标量 ←→ 逐请求模拟/固定分区） |
|---|---|---|---|
| vLLM | FCFS 端（`PRIORITY` 可选但不看前缀） | 激进端（preempt 默认开） | 静态标量端 |
| SGLang | 前缀感知端（LPM 可选，非默认） | 激进端（retract 默认开） | 介于两端之间（`new_token_ratio` 自适应折算） |
| TensorRT-LLM | 中间偏前缀感知（`enablePrefixAwareScheduling` 默认开，但只做二元判断） | 默认保守端（`GUARANTEED_NO_EVICT`），可调到激进端（`MAX_UTILIZATION`） | 静态标量端（KV block 计量） |
| LMDeploy | 中间（FCFS 基线 + 长 prefill 场景下的 age-credit 重排） | 激进端（`evict` 默认发生） | 静态标量端 |
| LightLLM | FCFS 端（不重排，只做准入门禁） | 保守端（`pause` 存在但准入本身已很保守） | 逐请求模拟端 |
| llama.cpp | 中间偏前缀感知（slot 选择用 LCP 相似度，但不重排队列） | 架构性不允许端 | 固定分区端 |
| TGI | FCFS 端（前缀缓存只减重算量） | 架构性不允许端 | 静态标量端 |
| Tokasaurus | FCFS 端（`greedy_prefill` 默认关闭重排） | 激进端（`bump` 默认可能发生） | 中间偏模拟端（`greedy_prefill=False` 时转向滚动模拟） |

这张表的读法：**没有一家引擎在三条轴上全部落在同一端**——即便是把"允许抢占"做到最激进的 SGLang，预算模型也没有走 vLLM 那种最简单的静态标量路线；即便是把"零中断"焊得最死的 TGI，前缀缓存本身依然是默认开启的。三条轴基本正交，组合出的姿态才是一个引擎真正的调度性格。

## 1. 它在系统里的位置

八家引擎的调度器都在解同一个三段式问题，只是把它拆分到不同的进程/线程边界里：

```
新请求进来 ──▶ [准入/排序]：谁先被考虑？看不看前缀缓存？
                    │
                    ▼
              [预算分配]：这一步谁能上场？吃几个 token？
                    │
                    ▼
              [资源不够时]：要不要牺牲已在跑的请求？牺牲谁？
```

这三段在源码里对应的位置，跨引擎差异很大——**这本身就是横向对比要先讲清楚的一层**，因为后面每一节的"实现位置"栏，读者需要知道自己在哪个进程/语言层里找：

| 引擎 | 调度决策跑在哪 | 与模型执行是否同进程 |
|---|---|---|
| vLLM | `EngineCore` 进程，纯 Python | 否，`AsyncLLM`/API 是另一进程（[[03-vLLM-调度器解剖]] `## 1`） |
| SGLang | `Scheduler` 进程，纯 Python | 否，三进程各自独立（[[02-SGLang-Scheduler事件循环]] `## 1`） |
| TensorRT-LLM | worker 进程内，C++（经 nanobind 暴露给 Python） | 是，Python 只转发调用（[[01-TensorRT-LLM]] `## 3.2`-`## 3.3`） |
| LMDeploy（PyTorch 后端） | 单进程，Python asyncio 事件循环（`lmdeploy:lmdeploy/pytorch/engine/engine.py:604`） | 是 |
| LMDeploy（TurboMind 后端） | 同进程内独立 C++ 线程（`lmdeploy:src/turbomind/engine/engine.cc:113`） | 是 |
| LightLLM | `RouterManager` 进程，纯 Python，ZMQ 与模型 TP worker 通信 | 否 |
| llama.cpp | 单进程，无独立调度线程——`update_slots()` 在主 HTTP 服务循环里跑 | 是 |
| TGI | Rust `router` 进程（tokio 异步任务） | 否，模型是独立 Python shard 进程（gRPC） |
| Tokasaurus | `manager` 进程，纯 Python | 否，模型 worker 是独立进程（`torch.multiprocessing`） |

三种典型形态：**调度与执行分进程**（vLLM/SGLang/LightLLM/TGI/Tokasaurus，好处是调度器崩溃不拖累 GPU 状态，坏处是每步都要跨进程/跨语言传结果）；**调度下沉到执行进程内的另一种语言**（TensorRT-LLM 的 C++、LMDeploy TurboMind 的 C++ 线程，好处是免掉跨进程序列化开销，坏处是改调度策略要走编译/绑定链路）；**调度与执行完全同进程同语言**（LMDeploy PyTorch 后端、llama.cpp，最简单但也最难做流水线重叠）。这条分类和 `## 2` 表格最后一列"并发模型"一一对应。

**还有一层容易被忽略的划分：谁决定"准入"（新请求何时从 waiting 变成 running），谁决定"续跑"（已在 running 的请求这一步该分多少 token）——这两件事不是所有引擎都放在同一个调用里做的。**

| 引擎 | 准入决策在哪 | 续跑决策在哪 | 是否同一次调用/同一进程 |
|---|---|---|---|
| vLLM | `schedule()` 的 WAITING 循环 | `schedule()` 的 RUNNING 循环 | 同一次 `schedule()` 调用 |
| SGLang | `get_new_batch_prefill()` | `update_running_batch()` | 同一次 `get_next_batch_to_run()` 调用，但两者互斥（`## 4.1`） |
| LightLLM | `req_queue.generate_new_batch()`，跑在 **router 进程**（`lightllm:lightllm/server/router/manager.py:429`-`431`） | `_get_classified_reqs()`，跑在**独立的模型 TP worker 进程**（经 `mp.Process` 启动，`lightllm:lightllm/server/router/model_infer/model_rpc.py:208`） | **不是**——是本篇覆盖的引擎里唯一把准入和续跑拆到两个不同 OS 进程的 |
| 其余五家 | 同一调度对象内部 | 同一调度对象内部 | 同进程/同调用 |

LightLLM 这个分工不是偶然的：`ChunkedPrefillQueue` 的最坏情况模拟（`## 5` 决策 4）只需要请求的元数据（预测输出长度、当前占用），不需要访问 GPU 状态，天然可以放在轻量的 router 进程里；而"这一步具体分几个 token、要不要 pause"需要读 KV 分配器的实时状态，只能放在持有显存的 TP worker 进程里。代价是准入决策用的"能不能放下"判断和续跑进程实际看到的显存状态之间存在一个时间差（router 算完 `generate_new_batch` 到 TP worker 真正执行之间），理论上可能出现准入时判断可行、执行时因为其它并发变化而事实上吃紧的情况——这也是为什么 LightLLM 需要 `## 5` 决策 2 的最坏情况模拟必须足够保守，以及为什么 `pause` 机制作为兜底依然存在。

## 2. 代码地图：跨引擎主对照表

下表是本篇的核心交付。**取材原则**：每个引擎只挑与本篇主题（调度器如何做排序/让位/预算/chunked prefill 决策）最相关的一条实现路径；LMDeploy 因为有 PyTorch 与 TurboMind 两个后端，主表填 PyTorch 后端（纯 Python，行为可对照读），TurboMind 只在"并发模型"与 `## 7` 提及。

| 引擎 | 排序策略 | 让位机制 | token 预算参数 | chunked prefill | 实现位置（文件:行） | 并发模型 |
|---|---|---|---|---|---|---|
| **vLLM** | FCFS(`deque`)/PRIORITY(`heapq`)二选一，均不读前缀缓存 | preempt，FCFS 下 `running.pop()`（LIFO），丢弃重算，插回等待队列**队头** | `max_num_scheduled_tokens`/`max_num_batched_tokens` 两个共享标量 | 默认开，块大小=当步剩余 token 预算（动态，非固定值） | `vllm:vllm/v1/core/sched/scheduler.py:484` | 2 进程（API/`AsyncLLM` + `EngineCore`） |
| **SGLang** | 默认 FCFS，可选 LPM/DFS-weight/LOF/RANDOM/ROUTING_KEY 共 6 种；等待队列 >128 自动退化 FCFS | retract，选"已生成 token 数最少者"，丢弃重算（单机模式），插回等待队列**队尾**（与新请求同等竞争） | `PrefillAdder.rem_total_tokens`（按 `new_token_ratio` 折算）+ `rem_chunk_tokens` | 默认开（`chunked_prefill_size`>0），超 `chunk_tokens_limit` 截断到 `trunc_len` | `sglang:python/sglang/srt/managers/scheduler.py:3080` | 3 进程（Tokenizer+API / Scheduler / Detokenizer） |
| **TensorRT-LLM** | 默认策略 `GUARANTEED_NO_EVICT` 不做前缀排序；`MAX_UTILIZATION` 按到达序装箱，`enablePrefixAwareScheduling`（默认 true）只做"该不该跳过"的二元判断 | "pause"，**仅 `MAX_UTILIZATION` 策略下发生**，从活跃列表末尾反向找最近启动的请求，丢弃 KV（`schedulingRemoveSequence`） | `maxNumRequests` + KV block 可用量（非纯 token 标量） | 由 `ContextChunkingPolicy` 决定切块顺序（首值 `FIRST_COME_FIRST_SERVED`），非简单开关 | `tensorrt-llm:cpp/tensorrt_llm/batch_manager/capacityScheduler.cpp:668` | Python 壳 + C++ 核心同进程；MPI 多进程做 TP/PP |
| **LMDeploy**（PyTorch） | FCFS（按 `arrive_time` 排序），可选 `_long_prefill_policy='size'` 对长 prefill 做带老化的重排 | 无专门术语，调 `seq.state.evict()`；victim = `running.pop(-1)`（running 已按 arrive_time 排序，pop 末尾=最新），丢弃重算；`swap_in_map`/`swap_out_map` 全程声明却从未被填充 | `cache_config.max_prefill_token_num`（默认 8192） | 无独立开关，长 prompt 按 `max_prefill_token_num` 自动切块 | `lmdeploy:lmdeploy/pytorch/paging/scheduler.py:841` | 单进程 asyncio（PyTorch）/ 同进程 C++ 线程（TurboMind） |
| **LightLLM** | FIFO（等待队列 list 顺序），准入靠逐请求"假设都跑到预测/最大长度"的最坏情况模拟，不重排 | "pause"（`pause_reqs`），victim 由迭代 `ready_reqs`（插入序）预算耗尽处触发，偏向更晚加入的请求；丢弃重算，每轮限流 `pause_max_req_num=2` | `batch_max_tokens`（chunked 模式下 ×2）+ `max_total_tokens` | 默认开（`disable_chunked_prefill=False`） | `lightllm:lightllm/server/router/req_queue/chunked_prefill/impl.py:54` | 多进程 ZMQ（router / detokenization / 模型 TP worker） |
| **llama.cpp** | 无请求间排序——固定 slot 池，`get_available_slot()` 按最长公共前缀（LCP）相似度（默认阈值 0.1）优先复用，否则退化到 LRU 选空闲 slot | 无抢占——所有 slot 都忙时任务被 `defer` 回队列等下一轮重试，不打断已在跑的 slot | `n_batch`（逻辑批预算，默认 2048）/ `n_ubatch`（物理子批，默认 512） | 隐含在 `n_ubatch` 的物理子批循环里，不是独立特性 | `llama.cpp:tools/server/server-context.cpp:1500` | 单进程，并发上限=启动时创建的固定 slot 数（server 默认 `-1`→自动 4） |
| **TGI** | 严格 FCFS（`VecDeque::pop_front`），前缀缓存（radix trie，默认开）只减少重算量，不参与排序 | **无请求级抢占**——分配失败就把请求 `push_front` 退回队列等下一轮，运行中的批次不受影响；`evict()` 只清未被引用的前缀缓存 block | `max_batch_prefill_tokens` + `max_batch_total_tokens` | `support_chunking` 开关，超预算请求按 `chunk_len` 截断塞入本批 | `tgi:backends/v3/src/queue.rs:237` | Rust 异步单进程（tokio）+ 独立 Python 模型 shard 进程（gRPC） |
| **Tokasaurus** | FIFO（`queued_seqs` 是保序 dict）；`greedy_prefill`（默认 True）关闭后转为 `calc_prefill_per_forward` 按未来 decode 完成时刻模拟 KV 占用曲线求解最优 prefill 速率（速率控制，非重排序） | "bump"，victim=`running_seqs.pop()`（LIFO），新建 id 丢弃原 KV（注释明写"don't redo completion tokens"=纯重算），插回等待队列**队头** | `max_tokens_per_forward`（默认 8192）/ `max_seqs_per_forward`（默认 1024） | 隐含于 `make_scheduling_decision` 按 `num_prefill` 额度逐请求分配 | `tokasaurus:tokasaurus/manager/scheduler.py:576` | 多进程 `torch.multiprocessing`（manager + model worker） |

**支撑引用**（`实现位置`列之外，本节正文额外核对过的行号，凑满硬指标要求的 10 条并留证据链）：

- vLLM 让位选谁：`vllm:vllm/v1/core/sched/scheduler.py:686`（`self.running.pop()`）；插回队头：`vllm:vllm/v1/core/sched/scheduler.py:1387`（`self.waiting.prepend_request(request)`）。
- SGLang 队列策略枚举：`sglang:python/sglang/srt/managers/schedule_policy.py:216`；默认值实测：`sglang:python/sglang/srt/server_args.py:873` 是 `"fcfs"`，不是常被误传的 `"lpm"`。
- LMDeploy 让位选谁：`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:829`（`running.pop(-1)`）；swap 字段声明处：`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:66`-`73`。
- LightLLM 让位触发处：`lightllm:lightllm/server/router/model_infer/mode_backend/base_backend.py:686`（`pause_max_req_num = 2`）与 `:760`（调用 `pause_reqs`）。
- llama.cpp 无抢占的证据：`llama.cpp:tools/server/server-context.cpp:2337`（`queue_tasks.defer(std::move(task))`）。
- TGI 无抢占的证据：`tgi:backends/v3/src/queue.rs:301`（`self.entries.push_front((id, entry))`，超预算请求原样退回队首）；prefix 缓存 evict 的对象是 block 不是请求：`tgi:backends/v3/src/radix.rs:363`。
- Tokasaurus 让位选谁：`tokasaurus:tokasaurus/manager/manager.py:764`（`running_seqs.pop()`）；插回队头：`tokasaurus:tokasaurus/manager/manager.py:799`（`insert_at_head_of_queued`）。
- TensorRT-LLM 默认策略：`tensorrt-llm:tensorrt_llm/llmapi/llm_args.py:3490`-`3491`（`default=CapacitySchedulerPolicy.GUARANTEED_NO_EVICT`）。

以上加上主表六列自带的 8 条引擎入口引用，本节合计 **21 条**带完整仓库根路径的 `文件:行` 引用，超过硬指标要求的 10 条。

**（附）Dynamo 不在主表里，但值得一句话交代它加在哪一层**：Dynamo 不做上面任何一件事——它没有自己的 `schedule()`。它在多个引擎实例之上加了一层 **KV-aware routing**：`RadixTree`（`dynamo:lib/kv-router/src/indexer/radix_tree.rs:49`）维护"哪个 worker 持有哪些 KV block hash"，`KvEventPublisher`（`dynamo:lib/llm/src/kv_router/publisher/mod.rs:190`）把各 worker 的前缀缓存事件汇总上来，`DefaultWorkerSelector::worker_cost`（`dynamo:lib/kv-router/src/scheduling/selector/default.rs:333`）把"前缀命中了多少块"和"这个 worker 有多忙"合成代价函数选路。这是"选哪个引擎实例处理这个请求"的问题，本篇讨论的是"选中之后引擎内部怎么调度"，两者是正交的两层，详见 [[09-NVIDIA-Dynamo]]。

### 2.1 逐引擎速记：主表之外再各补一条

主表每格只能放一个事实，下面给每家引擎补一条主表放不下、但读者动手改配置前应该知道的细节：

- **vLLM**：`self.waiting`/`self.skipped_waiting` 是两个独立的队列容器，共享同一套排序策略——`skipped_waiting` 不是第二套排序规则，只是"本步被跳过的请求"的临时存放处（`vllm:vllm/v1/core/sched/scheduler.py:196`-`198`）。
- **SGLang**：`waiting_queue` 是一个普通 Python `list`（`sglang:python/sglang/srt/managers/scheduler.py:1144`），不是任何专用队列类——LPM/FCFS 等排序策略在读取它时原地 `sort()`，不是维护一个持续有序的结构。
- **TensorRT-LLM**：活跃请求容器是 `RequestList = std::list<std::shared_ptr<LlmRequest>>`（`tensorrt-llm:cpp/include/tensorrt_llm/batch_manager/common.h:36`）——用双向链表而不是 vector，这与 `## 5` 决策 3 里"从末尾反向查找"的访问模式是匹配的设计（链表的尾部删除是 O(1)）。
- **LMDeploy**：`waiting`/`ready`/`running`/`hanging` 都不是独立容器，而是通过 `create_status_list_property()`（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:630`-`638`）动态从 `seq_manager` 按 `MessageStatus` 过滤出来的属性——同一个请求对象在不同状态间流转时不需要"从一个队列搬到另一个队列"，只需要改一个状态位。
- **LightLLM**：`waiting_req_list: List[Req]` 是最朴素的 Python list（`lightllm:lightllm/server/router/req_queue/base_queue.py:29`），`ChunkedPrefillQueue` 每次准入检查都要重新遍历它并做 `numpy` 向量化计算（`## 5` 决策 4），不是维护增量状态。
- **llama.cpp**：`queue_tasks`/`queue_tasks_deferred` 是两个独立的 `std::deque<server_task>`（`llama.cpp:tools/server/server-queue.h:24`-`25`）——被 `defer` 的任务进的是**另一个**队列，不是原队列的队尾，这一点和本篇 `## 2` 表格里"defer 回队列"的简化描述略有差别，值得读者动手改代码前注意。
- **TGI**：`entries: VecDeque<(u64, Entry)>`（`tgi:backends/v3/src/queue.rs:168`）用 `u64` id 而不是直接存 `Entry`，配合 `nohash_hasher`（`tgi:backends/v3/src/queue.rs:6`）——这是一个纯性能细节（避免为顺序自增 id 做哈希），但说明 TGI 团队把这条队列当热路径在打磨。
- **Tokasaurus**：`queued_seqs: dict[str, Sequence]`（`tokasaurus:tokasaurus/manager/types.py:165`）用 dict 而不是 list/deque 实现 FIFO——依赖的是 Python 3.7+ 字典保留插入顺序的语言特性，`insert_at_head_of_queued()`（`## 5` 决策 3 已引用）能做到"整体前插"正是因为它是重建一个新 dict 而不是链表操作。

### 2.2 体量对照——vLLM 4,012 行 vs Tokasaurus 684 行，多出来的行数在做什么

任务书点名这条体量差是个好抓手，本篇实测核对了八家引擎"调度直接相关文件"的行数（`wc -l` 实测，非 `struct_map.json` 的关键词匹配估算，避免重蹈 [[03-vLLM-调度器解剖]] `## 2` 踩过的"文件名匹配假阳性"覆辙）：

| 引擎 | 核心调度文件 | 行数 | 备注 |
|---|---|---|---|
| vLLM | `vllm/v1/core/sched/` 整个目录 6 个文件 | **4,012** | 已在 [[03-vLLM-调度器解剖]] `## 2` 核实过范围边界 |
| SGLang | `scheduler.py` 单文件 | **5,228** | 含 PP/Multiplex/PD 分离等多个 mixin，不是纯"连续批处理"逻辑 |
| TensorRT-LLM | `capacityScheduler.{cpp,h}` + `microBatchScheduler.cpp` | **1,480** | C++ 实现，不含被它调用的 `KVCacheManager` |
| LMDeploy | `paging/scheduler.py` 单文件 | **986** | PyTorch 后端，不含 TurboMind（C++，另计） |
| LightLLM | 准入层 `base_queue.py`+`chunked_prefill/impl.py` | **244** | 只是队列/准入这一部分；per-step 分类与 pause 逻辑在 `base_backend.py`（1,006 行）里，但那个文件还混着模型执行相关代码，本库未拆分出纯调度行数（未查证精确占比） |
| llama.cpp | `server-context.cpp` 全文件 | 5,475 | **陷阱**：这个数字几乎不能代表"调度"，见下文 |
| TGI | `queue.rs`+`backend.rs` | **1,408** | 不含 `radix.rs`（1,041 行，前缀缓存驱逐，是独立关注点） |
| Tokasaurus | `scheduler.py` 单文件 | 684 | **陷阱**：bump/preemption 逻辑不在这个文件里，见下文 |

**两个需要专门拆穿的陷阱**，都出现在"文件行数"这个指标最容易骗人的地方：

- **llama.cpp 的 5,475 行几乎不能代表调度**。`server-context.cpp` 是整个 HTTP server 的核心实现文件，里面混着多模态处理、tokenize/detokenize、CUDA Graph 之外的一切服务逻辑；真正与本篇主题相关的 `get_available_slot()`（`llama.cpp:tools/server/server-context.cpp:1500`-`1567`）和 `update_slots()`（`:2723` 起）两个函数本身只有几十到一两百行的量级。**这是本篇要提醒读者的通用陷阱**：不能拿"调度逻辑所在的文件有多大"当"调度逻辑本身有多复杂"的代理指标——llama.cpp 恰恰是反例：文件很大，调度逻辑很薄，因为固定 slot 模型天生不需要复杂的仲裁代码（`## 4.3`）。
- **Tokasaurus 的 684 行低估了完整图景**。任务书给的"684 行"只是纯算法文件 `scheduler.py`（`schedule()`/`calc_prefill_per_forward()`/`make_scheduling_decision()`），但本篇反复引用的 bump/preemption 逻辑（`allocate_tokens_for_decode_bumping_seqs_if_necessary()`）实际上在**另一个文件** `manager.py`（1,064 行）里（`tokasaurus:tokasaurus/manager/manager.py:746`-`799`）。两个文件合计 1,748 行，比单看 `scheduler.py` 的印象要厚实得多——**684 行确实是"决定怎么切 prefill/decode 的核心算法"的真实体量，但"完整的连续批处理调度"（含准入、抢占、状态维护）从来不只是这一个文件的事**，这条边界和 vLLM 篇讲"6 文件 4,012 行"时特意划清"调度器范围边界"是同一类严谨性要求。

真正体量差距明显、且不掉进上面两个陷阱的对比是 **SGLang（5,228 行单文件）vs LMDeploy（986 行单文件）vs TGI（1,408 行两文件）**——三者统计口径相近（都是"调度决策直接相关的文件"，没有把明显不相关的功能塞进来），SGLang 比 LMDeploy 厚 5 倍以上。多出来的行数在做什么？本篇 `## 2` 代码地图与 [[02-SGLang-Scheduler事件循环]] `## 2` 已经给出答案：SGLang 的 `Scheduler` 类（`sglang:python/sglang/srt/managers/scheduler.py:384`）同时承载 PP 流水线并行、PD 分离的两种角色（prefill worker/decode worker）、multiplex 多路复用、结构化输出的语法同步等多个 mixin（`SchedulerPPMixin`/`SchedulerMultiplexMixin`/`SchedulerDisaggregationDecodeMixin`/`SchedulerDisaggregationPrefillMixin`），LMDeploy 的 `Scheduler` 只做本篇讨论的这一件事——**体量差距的大头不是"同一个功能写得啰嗦"，是"塞进了多少功能"**，这条结论和 vLLM 篇里"`SchedulerOutput` 14 个字段大多数服务旁路系统"是同一个规律在两个不同维度上的重复出现。

### 2.3 配置旋钮速查

本篇正文散落提到了近三十个具体配置项，这里汇总成一张表方便按引擎查——**只收本篇正文里已经带引用核实过的旋钮，不额外补充未讨论过的配置项**：

| 引擎 | 配置项 | 默认值 | 一句话作用 | 本篇出处 |
|---|---|---|---|---|
| vLLM | `enable_chunked_prefill` | 开 | 是否允许把长 prompt 切块跨步计算 | `## 2` 主表 |
| vLLM | `long_prefill_token_threshold` | `0`（=不额外限制） | 长 prefill 的专用裁剪上限，默认关闭 | [[03-vLLM-调度器解剖]] `## 7` |
| vLLM | `watermark` | `0.0`（关闭） | 给续期请求预留的显存水位线 | [[03-vLLM-调度器解剖]] `## 7` |
| vLLM | `scheduler_reserve_full_isl` | `True` | 准入时要求整条序列都能塞进 KV cache，不只是第一块 | [[03-vLLM-调度器解剖]] `## 7` |
| SGLang | `schedule_policy` | `"fcfs"` | 等待队列排序策略，六选一 | `## 5` 决策 1 |
| SGLang | `chunked_prefill_size` | 非 `-1`（开） | chunked prefill 单块 token 上限 | `## 4.2` |
| SGLang | `enable_mixed_chunk` | `False` | 是否允许 prefill 与 decode 混进同一物理批 | `## 4.1` |
| SGLang | `prefill_decode_interval` | `0`（不设限） | 跑几轮 decode 才能再插一次 prefill | [[02-SGLang-Scheduler事件循环]] `## 5` 决策一 |
| TensorRT-LLM | `capacity_scheduler_policy` | `GUARANTEED_NO_EVICT` | 要不要允许 pause 已在跑请求 | `## 5` 决策 2 |
| TensorRT-LLM | `enablePrefixAwareScheduling` | `True` | 前缀命中率是否影响"这个请求该不该跳过" | `## 0`、`## 5` 决策 1 |
| TensorRT-LLM | `context_chunking_policy` | `None`（解析路径未追踪） | 多个长 prompt 之间怎么分 chunk 预算 | `## 4.2`、`## 6` |
| LMDeploy | `eviction_type` | `'recompute'`（唯一实现） | 让位时 KV 怎么处理 | `## 7` |
| LMDeploy | `cache_config.max_prefill_token_num` | `8192` | 单步 prefill token 预算 | `## 2` 主表 |
| LMDeploy | `opt_ttft_policy`（`LMDEPLOY_PT_TTFT_POLICY`） | `'size'` | 长 prefill 是否按预计切块数重排 | `## 5` 决策 6 |
| LMDeploy | `opt_ttft_aging_sec`（`LMDEPLOY_PT_TTFT_AGING_SEC`） | `2.0` 秒 | age credit 每个"信用点"对应的等待秒数 | `## 5` 决策 6 |
| LightLLM | `disable_chunked_prefill` | `False`（=开） | 是否禁用 chunked prefill | `## 2` 主表 |
| LightLLM | `batch_max_tokens` | 无内置默认，需显式指定 | 单批 prefill token 预算（chunked 模式 ×2） | `## 2` 主表 |
| LightLLM | `pause_max_req_num` | `2`（硬编码，非可配置旋钮） | 每轮最多暂停几个请求 | `## 3.1` |
| llama.cpp | `--parallel`/`-np` | `-1`→server 自动解析为 `4` | 固定 slot 数，即并发上限 | `## 4.3` |
| llama.cpp | `--batch-size`/`-b`（`n_batch`） | `2048` | 逻辑批 token 预算 | `## 2` 主表 |
| llama.cpp | `--ubatch-size`/`-ub`（`n_ubatch`） | `512` | 物理子批大小 | `## 2` 主表 |
| llama.cpp | `--slot-prompt-similarity` | `0.1` | LCP 相似度阈值，决定要不要复用某个 slot | `## 7` 反直觉 4 |
| TGI | `support_chunking` | 视模型/后端支持而定 | 新 prefill 能否与已有 batch 拼接跑 | `## 2` 主表 |
| TGI | `max_batch_prefill_tokens` | 无内置默认，需显式指定 | prefill 阶段 token 预算 | `## 2` 主表 |
| TGI | `PREFIX_CACHING`（launcher 环境变量） | `"true"`（`tgi:launcher/src/main.rs:196`） | 是否启用 radix trie 前缀缓存 | `## 2` 主表 |
| Tokasaurus | `greedy_prefill` | `True` | 是否直接榨干所有可 prefill 的请求 | `## 7` 反直觉 5 |
| Tokasaurus | `max_tokens_per_forward` | `8192` | 单步 token 预算 | `## 2` 主表 |
| Tokasaurus | `max_seqs_per_forward` | `1024` | 单步并发请求数上限 | `## 2` 主表 |

**读这张表的方式**：它不是一份"调优指南"（本篇不产出性能数字，不能告诉你调哪个值能提升多少吞吐），而是一份"改配置前先搞清楚默认行为"的核对清单——本篇 `## 7` 的多条反直觉发现，几乎都是"某个配置项的默认值和直觉不符"，这张表把它们集中到了一处方便复查。

## 3. 核心数据结构：跨引擎术语翻译表

单引擎解剖篇的 `## 3` 讲的是一个引擎内部的类；本篇是跨引擎横向对比，"核心数据结构"改讲一件更有用的事——**同一个概念在八家引擎里叫什么名字、对应哪个类/函数**，避免读者在切换源码时被术语绊倒。

### 3.1 "牺牲一个正在跑的请求"这件事的八种叫法、八套细节

这是本篇必须回答的第 3 项——不能只讲两家（vLLM/SGLang）就收尾，下表把八家引擎的让位机制拆成五个可比较的维度：叫什么、谁选受害者、放回队列哪个位置、KV 怎么处理、有没有安全阀。

| 引擎 | 术语 | 选谁（victim 规则） | 放回队列哪里 | KV 怎么处理 | 安全阀 |
|---|---|---|---|---|---|
| vLLM | preemption | FCFS 下 `running.pop()`（LIFO）；PRIORITY 下 `(priority, arrival_time)` 最大者 | **队头**（`prepend_request`） | 丢弃重算，V1 无 swap 路径 | 无显式上限，理论上会一直抢占到评估中的请求自己被抢占为止 |
| SGLang | retraction | `_get_decode_retraction_order`：已生成 token 数最少者优先 | **队尾**（与新请求同等竞争） | 单机丢弃重算；`decode disaggregation` 模式下 host 备份（`sglang:python/sglang/srt/managers/schedule_batch.py:1932`-`1940`） | 有：至少保留一个请求（`if len(sorted_indices) == 1: break`）；若最后一个仍放不下则直接 `FINISH_ABORT` |
| TensorRT-LLM | pause（仅 `MAX_UTILIZATION`） | 从活跃列表末尾反向查找最近启动者 | 不入队列，留在 active 列表里等下次被重新纳入调度 | 丢弃重算（`schedulingRemoveSequence`），未见 host 备份分支 | 反向查找到 `reqIt`（评估中的请求）为止，找不到就整体 `break` |
| LMDeploy | 无专名，代码里叫 `evict` | `running.pop(-1)`（running 已按 arrive_time 排序，取最新） | 回到 `WaitingState`，按 arrive_time 重新参与排序（结构上接近队尾） | 丢弃重算；`swap_in_map`/`swap_out_map` 字段声明但从未填充（`## 7`） | 循环驱逐至 `running` 空；若仍不够，评估中的 `seq` 自身被 evict（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:832`-`834`） |
| LightLLM | pause | 迭代 `ready_reqs`（插入序）预算耗尽处触发，偏向更晚加入者 | 保留在 `paused` 状态，同一进程内按 `can_alloc_token_num` 恢复（不入等待队列） | 丢弃重算（`free_a_req_mem`），恢复时 `_match_radix_cache` 可命中前缀 | 每轮限流 `pause_max_req_num=2`（`lightllm:.../base_backend.py:686`），避免一次性暂停过多 |
| llama.cpp | 不存在 | — | — | — | 不适用，架构上没有这个操作 |
| TGI | 不存在（请求级） | — | — | — | 不适用；`evict()` 只作用于无引用的前缀缓存 block |
| Tokasaurus | bump | `running_seqs.pop()`（LIFO） | **队头**（`insert_at_head_of_queued`） | 新建 id 丢弃原 KV，注释明写不做增量续算（纯重算） | 循环驱逐至满足 `num_needed_blocks <= num_free_blocks`，无显式上限 |

**读这张表要避免的误解**：不要把"术语不同"等同于"行为随意"。八个名字背后只有两种真实语义——**允许丢弃重算**（vLLM/SGLang/TRT-LLM/LMDeploy/LightLLM/Tokasaurus 六家，只是谁来选、选谁、放回哪里各不相同，见 `## 5` 决策 2/3）和**架构上不存在这个操作**（llama.cpp/TGI 两家，靠更保守的准入控制回避这个问题）。

**放回队列的位置这一列值得单独强调**：vLLM 和 Tokasaurus 都选择插回**队头**（受害请求下一步优先重试），SGLang 选择插回**队尾**（与新请求平等竞争，`## 5` 决策 2 已在 [[02-SGLang-Scheduler事件循环]] 里指出这是它"没有插队特权"的设计），LMDeploy 的效果接近队尾（按 arrive_time 重新排，不获得特殊优待）。**放回队头 vs 队尾直接决定了受害请求的恢复延迟是否有上界**——队头意味着"下一步大概率被优先捡起"，队尾意味着"恢复时间不确定，取决于新请求到达速率"，这条差异比"用什么词描述这个操作"更值得工程师关心。

### 3.2 每家引擎"一次调度决策"的核心返回类型

这组类型决定了调度器与执行层之间的契约边界，横向看能发现一个共性：**返回类型越丰富，说明这个调度器承担的职责越多**。

| 引擎 | 类型 | 字段数量级 | 备注 |
|---|---|---|---|
| vLLM | `SchedulerOutput`（`vllm:vllm/v1/core/sched/output.py:206`） | 14 个顶层字段 | 见 [[03-vLLM-调度器解剖]] `## 3.2`，多数字段服务 KV connector/投机解码等旁路系统 |
| SGLang | `NextBatchPlan`（`sglang:python/sglang/srt/managers/schedule_batch.py:3429`） | 2 个字段 | 只有 `batch_to_run`/`running_batch`，调度决策与状态更新显式分离 |
| TensorRT-LLM | 三元组 `(scheduled, paused, fitting_disagg_gen_init)` | 3 个 | 原始 C++ 返回签名，Python 只转发，不是 dataclass 契约 |
| LMDeploy | `SchedulerOutput`（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:67`） | 4 个字段 | `running`/`swap_in_map`/`swap_out_map`/`copy_map`——后两个字段是 `## 7` 要讲的死代码 |
| LightLLM | `Batch`（新建的请求列表对象） | 1 个（请求列表） | 无独立"本步决策"结构，`generate_new_batch` 直接返回可跑的 `Batch` |
| llama.cpp | 无——直接操作 `slots` 数组 | — | 没有把"这一步决策"物化成独立对象，`update_slots()` 就地改状态 |
| TGI | `(entries, batch, span)` 元组 | 3 个 | Rust 侧同样是元组而非结构体 |
| Tokasaurus | `ScheduleDecision`（`tokasaurus:tokasaurus/manager/types.py:254`） | 3 个字段 | `decoding_seqs`/`prefill_seqs`/`id` |

vLLM 和 SGLang 的字段数量差异（14 vs 2）不是"谁设计得更好"，而是 **vLLM 把 KV connector、投机解码、结构化输出等旁路系统的通信也塞进了同一个契约对象**；SGLang 把这些旁路信息通过其它通道（比如直连 ZMQ socket）传递，`NextBatchPlan` 只承载最核心的批次决策。两种做法在 [[03-vLLM-调度器解剖]] `## 5` 决策 6 与 [[02-SGLang-Scheduler事件循环]] `## 5` 决策三分别有更深的讨论。

### 3.3 等待队列的底层容器类型

`## 2.1` 已经逐条提过，这里汇总成一张表，方便按"我要改这段代码"去精确定位：

| 引擎 | 等待队列的底层类型 | 出队方式 | 时间复杂度特征 |
|---|---|---|---|
| vLLM | `deque`（FCFS）/`heapq`（PRIORITY） | `popleft()` / 堆弹出 | O(1) / O(log n) |
| SGLang | 普通 `list` | 每次调度前原地 `sort()` | 每步 O(n log n)，无持续有序结构 |
| TensorRT-LLM | `std::list<std::shared_ptr<LlmRequest>>` | 反向迭代器查找 | 双向链表，尾部增删 O(1) |
| LMDeploy | 无独立容器，`MessageStatus` 属性过滤 | 每次访问 `self.waiting` 重新过滤 | 依赖 `seq_manager` 内部索引结构，本篇未深挖 |
| LightLLM | 普通 `list` | 逐个遍历 + numpy 批量计算 | 准入检查每次 O(n)（`n`=等待队列长度） |
| llama.cpp | 两个独立 `std::deque<server_task>`（`queue_tasks`/`queue_tasks_deferred`） | `pop_front` 语义 | O(1)，但两个队列语义不同（见 `## 2.1`） |
| TGI | `VecDeque<(u64, Entry)>` | `pop_front()` | O(1) |
| Tokasaurus | `dict[str, Sequence]`（保序） | 消费 `deque(dict.values())` 或整体重建 | O(1) 均摊，前插靠重建新 dict |

**这张表的价值在于纠正一个常见的性能直觉误区**：不要假设"用了 `list` 就一定慢"——SGLang 和 LightLLM 都用普通 list，但它们的等待队列长度在正常部署下通常是几十到几百量级（SGLang 超过 128 就自动退化策略，`## 5` 决策 1），O(n log n)/O(n) 的操作在这个规模下不是瓶颈；反过来也不要假设"用了 O(1) 结构就一定快"——TGI/Tokasaurus 的 O(1) 出队要配合它们各自的准入检查逻辑一起看，真正的开销可能在别处（比如 block allocator 的分配尝试）。

## 4. 主流程走读：prefill/decode 怎么二选一，固定 slot vs 动态调度

### 4.1 判据对照：这一步是 prefill 还是 decode，谁来决定

八家引擎在这个问题上分两大阵营：**无阶段区分**（一个批次天然是 prefill 和 decode 的混合体，由 token 预算决定谁能进批）和**批类型互斥**（一轮迭代要么是纯 prefill 批要么是纯 decode 批，由"是否存在可调度的新 prefill"这个布尔判据决定）。

| 引擎 | 阵营 | 判据位置 | 混部 prefill/decode 的方式 |
|---|---|---|---|
| vLLM | 无阶段区分 | `vllm:vllm/v1/core/sched/scheduler.py:487`-`495`（源码注释原话："There's no 'decoding phase' nor 'prefill phase'"） | 天然混部，每请求独立追赶 `num_computed_tokens` |
| SGLang | 批类型互斥（默认） | `sglang:python/sglang/srt/managers/scheduler.py:3191`-`3200`（"Run prefill first if possible"） | 仅 `enable_mixed_chunk=True` 时通过 `ForwardMode.MIXED` 混部，默认关闭 |
| TensorRT-LLM | 两级调度：`CapacityScheduler` 决定谁能进，`MicroBatchScheduler` 再拆 context/generation 批 | `tensorrt-llm:tensorrt_llm/_torch/pyexecutor/_util.py:3082`-`3094` | in-flight batching 允许同批混部，具体由 C++ 两级调度器共同决定 |
| LMDeploy | 批类型互斥 | `lmdeploy:lmdeploy/pytorch/paging/scheduler.py:841`-`850`（`schedule(is_prefill: bool, ...)` 显式传入布尔） | 上层 engine 循环显式在 prefill/decode 两个调用之间切换，不在调度器内部混部 |
| LightLLM | 无阶段区分 | `lightllm:lightllm/server/router/model_infer/mode_backend/base_backend.py:718`（`is_decode = req_obj.cur_kv_len + 1 == req_obj.get_cur_total_len()`） | 天然混部，同一轮 `prefill_reqs`/`decode_reqs` 两个列表可以同时非空 |
| llama.cpp | 无阶段区分（slot 级） | `llama.cpp:tools/server/server-context.cpp:2723`（`update_slots()`） | 同一次 `llama_decode` 调用里不同 slot 可以分别处于 prefill/decode 状态，物理上混部在一个 batch 里 |
| TGI | 无阶段区分（chunked 时） | `tgi:backends/v3/src/backend.rs:221`-`231`（`support_chunking` 分支：`entries.extend(new_entries)` 后一起 `prefill()`） | 支持 chunking 时新 prefill 与旧 cached batch 拼接一起跑；不支持时退化为"先等新 batch 跑完首 token 再拼" |
| Tokasaurus | 无阶段区分（预算共享） | `tokasaurus:tokasaurus/manager/scheduler.py:600`-`605`（`max_prefill = max_tokens_per_forward - len(decoding_seqs)`） | decode 先扣预算，剩余预算给 prefill，同批天然混部 |

**规律**：把执行状态放在**单请求**粒度追踪的引擎（vLLM、LightLLM、Tokasaurus、TGI-chunking、llama.cpp 的 slot 级）天然支持混部；把执行状态放在**批**粒度追踪的引擎（SGLang 默认、LMDeploy）需要显式的"是否混部"开关才能打破互斥，这不是谁更先进，而是数据结构的选择决定了混部是不是"顺手"的事——批粒度状态机的代码更好读（`ForwardMode` 一个枚举说清楚这一步是什么），代价是要多写一条 `MIXED` 分支才能拿到混部的吞吐收益。

### 4.2 chunked prefill 默认开关与块大小对照

| 引擎 | 默认开关 | 块大小怎么定 |
|---|---|---|
| vLLM | 开（`enable_chunked_prefill`） | 无固定值——当步共享 `token_budget` 剩多少切多少，`long_prefill_token_threshold` 默认 `0`=不额外限制 |
| SGLang | 开（`chunked_prefill_size` 默认非 -1） | `chunk_tokens_limit`，超出截断到页对齐的 `trunc_len` |
| TensorRT-LLM | 由 `ContextChunkingPolicy` 决定顺序而非开关；具体块大小受 KV/token 约束联合决定 | 未在本篇深挖单块大小公式（未查证具体默认字节数） |
| LMDeploy | 无独立开关，架构性地总是按预算切 | `cache_config.max_prefill_token_num`（默认 8192），多模态场景可能更大 |
| LightLLM | 开（`disable_chunked_prefill` 默认 `False`） | 由 `batch_max_tokens` 与预算模拟共同决定，无固定块大小常量 |
| llama.cpp | 无独立特性——`n_ubatch` 本身就是每次物理前向的粒度 | `n_ubatch`（默认 512） |
| TGI | `support_chunking` 由部署方模型决定是否开启 | `prefill_token_budget - prefill_tokens`（动态，剩多少切多少，类似 vLLM） |
| Tokasaurus | 隐含，无独立开关 | 由 `calc_prefill_per_forward` 算出的 `num_prefill` 逐请求分配，`prefill_round_up_multiple`（默认 16）对齐 |

**共性**：除 llama.cpp（块大小=物理批大小，与"避免长 prompt 独占一步"的动机无关）之外，**所有支持 chunked prefill 的引擎都把"避免一个长 prompt 独占一整步、拖慢并发 decode 请求的 token 间延迟"作为唯一动机**——这条动机在 vLLM/SGLang/TGI 三家的源码注释里都有直接或间接的表述，llama.cpp 没有这个概念是因为它的并发上限被固定 slot 数原生限制住了，本来就不存在"一个请求占满一整步预算"这种情况。

**TensorRT-LLM 的 `ContextChunkingPolicy` 值得单独展开**，因为它是本篇覆盖的引擎里唯一把"多个长 prompt 同时排队时，先紧着谁的 chunk 切"做成显式可选策略的：`kFIRST_COME_FIRST_SERVED`（`tensorrt-llm:cpp/include/tensorrt_llm/executor/types.h:239`-`240`，docstring 原话"Sequential chunking, complete the unfinished context phase first"）会把预算集中喂给队首那一个长 prompt，让它尽快切完；`kEQUAL_PROGRESS`（`tensorrt-llm:cpp/include/tensorrt_llm/executor/types.h:241`-`243`，"Iterate through each context request in sequence and attempt to increase its chunk count until the constraint is exceeded"）反过来把预算雨露均沾地摊给所有排队的长 prompt，让它们的 TTFT 大致同步推进而不是一个接一个地"谁先来谁先吃完"。vLLM/SGLang/TGI 的 chunked prefill 都没有这个"多个长请求之间怎么分预算"的二级策略——本篇通读代码确认它们默认行为等价于 `FIRST_COME_FIRST_SERVED`（谁在预算循环里先被遍历到，谁先吃满这一步的份额），但没有把"要不要换成雨露均沾"做成可配置项。

### 4.3 固定 slot（llama.cpp）vs 动态调度（其余七家）

llama.cpp 是本篇八家引擎里唯一一个不做"动态调度"的：

- **动态调度七家**的共同前提是"KV cache 是一个可以按 block/page 细粒度分配的池子"，调度器每步决定这个池子怎么在请求间重新分配。
- **llama.cpp** 反过来：先按 `--parallel`/`-np`（server 默认 `-1`→自动 `4`，`llama.cpp:tools/server/server.cpp:152`-`156`）固定切出 N 个 slot，每个 slot 独立持有自己的 KV 状态，调度问题退化成"选一个空闲/前缀相似的 slot"（`llama.cpp:tools/server/server-context.cpp:1500`），不存在跨 slot 的资源重新分配。

两种模型的根本差别是**内存管理粒度**：动态调度以 block/page（通常 16-256 token）为单位管理显存，允许并发数随请求实际显存占用弹性伸缩；固定 slot 以"整条序列的最大长度"为单位预留显存（每个 slot 预留 `n_ctx / n_parallel`），并发数在启动时就写死，与实际请求长度分布无关。`## 5` 决策 5 用三样法则展开这个选择的代价。

### 4.4 一步之内，谁先花预算——八家引擎的消耗顺序

`## 4.1` 讲的是"这一步整体算 prefill 还是 decode"，这里更进一步：**在同一步预算够但不够所有人的情况下，谁先拿到份额**。这条顺序决定了"预算紧张时，decode 的 token 间延迟和新请求的 TTFT 哪个先被牺牲"。

| 引擎 | 预算消耗顺序 | 后果 |
|---|---|---|
| vLLM | RUNNING（含 decode 与未完成 chunked prefill）全部先扫一遍，剩余预算才轮到 WAITING（`vllm:vllm/v1/core/sched/scheduler.py:531`-`753` 先于 `:755`-`1176`） | 已在跑的请求（含 decode）优先，新请求 TTFT 可能因老请求占满预算而变长（[[03-vLLM-调度器解剖]] `## 5` 决策 1） |
| SGLang | 有新 prefill 批可组就无条件先跑它，decode 只在"没有新 prefill 可跑"时兜底（`sglang:python/sglang/srt/managers/scheduler.py:3191`-`3200`） | 与 vLLM 完全相反：新请求 TTFT 优先，decode 的 token 间隔可能被持续到达的新 prefill 无限期推迟（`prefill_decode_interval` 默认 `0`=不设限） |
| TensorRT-LLM | 两级调度：`CapacityScheduler` 先决定谁能进这一步的活跃集合，`MicroBatchScheduler` 再按 `ContextChunkingPolicy` 分配 context（prefill）份额，generation（decode）请求通常独立成批 | 具体优先级取决于两级调度器的策略组合，本篇未追踪全部组合下的确定性顺序（未查证） |
| LMDeploy | 外层 engine 循环显式传入 `is_prefill` 布尔，`_schedule_prefill`/`_schedule_decoding` 二选一执行，不在同一步内竞争同一份预算 | prefill 和 decode 完全分属不同的调度调用，谁先谁后由上层引擎循环的调用顺序决定，调度器本身不做取舍 |
| LightLLM | 单次遍历 `ready_reqs`（插入序），逐请求判断 `is_decode` 后立即扣减共享的 `can_alloc_token_num`——**没有"decode 全部先来"或"prefill 全部先来"的整体阶段**，谁在列表里靠前谁先拿 | 效果上偏向"先加入 running 的请求"（不分 prefill/decode），与到达顺序绑定 |
| llama.cpp | 无共享预算——每个 slot 独立更新，`update_slots()` 在一次 `llama_decode` 调用里把所有活跃 slot 的 token 一起提交 | 不存在"谁先花预算"的问题，slot 数量本身就是硬上限 |
| TGI | 已有的 cached batch（decode）总是继续；新 prefill 能否追加取决于 `support_chunking`——开启时与当前 batch 拼接一起跑，关闭时新 prefill 要等当前批处理完 | 默认（`support_chunking` 视模型支持而定）行为接近 vLLM：已在跑的优先，新请求排队 |
| Tokasaurus | 显式公式 `max_prefill = max_tokens_per_forward - len(queue.decoding_seqs)`（`tokasaurus:tokasaurus/manager/scheduler.py:598`）——**decode 先无条件拿到 1 个名额/请求，剩余预算才给 prefill** | 与 vLLM 同向：decode 优先，且是本篇里唯一把这条优先级写成一行显式算术公式（而不是隐含在循环顺序里）的引擎 |

八家里，**vLLM/Tokasaurus/TGI（默认）站"decode 优先"一边，SGLang 站"prefill 优先"一边，LightLLM 不分阵营（按到达顺序统一竞争），LMDeploy/llama.cpp 因为架构原因不存在这个竞争**。这条分野和 `## 4.1` 的"批类型是否互斥"高度相关但不是同一回事——TGI 支持 chunking 时批类型不互斥，但优先级依然明确偏向已在跑的 decode 请求。

### 4.5 统一场景走查：KV cache 基本占满时来了一个新请求 D

前面几节是逐维度横向对比，这里换一个角度——**固定一个场景，把八家引擎的反应并排放在一起**，检验前面几张表讲的机制在同一个触发点上到底怎么联动。场景设定：多个请求已经在跑，KV cache／slot 占用接近上限，且没有任何在跑请求恰好在这一步结束，这时来了一个新请求 D。这是一个纯机制走查（不代入具体 token 数字，`_PLAN.md` 红线不产出这类拼凑的性能场景），每一步都能对应回本篇前面已给出的引用。

- **vLLM**：D 在 WAITING 循环里被考虑，`allocate_slots` 返回 `None`；FCFS 下触发抢占，`self.running.pop()` 弹出**最后加入 running 的请求**（`vllm:vllm/v1/core/sched/scheduler.py:686`），丢弃其 KV，插回等待队列**队头**（`:1387`）。若这样腾出的空间仍不够 D，继续抢占下一个，直到 D 自己成为被抢占对象为止——D 这一步大概率能挤进去，代价是牺牲了别人。
- **SGLang**：先看 `get_new_batch_prefill()` 能不能凑出含 D 的新 prefill 批；如果排序策略是 LPM 且 D 与树中内容毫无重叠，D 可能排在队尾迟迟轮不到（`## 5` 决策 1）。就算轮到 D，若显存不够，走的是 `update_running_batch()` 里的 `retract_decode()`——退的是"已生成 token 数最少者"，插回等待队列**队尾**（`sglang:python/sglang/srt/managers/scheduler.py:2790`），与 D 自己重新平等竞争。
- **TensorRT-LLM**：默认 `GUARANTEED_NO_EVICT` 下，D 只有在准入时的保守估计通过才会被接纳，否则直接不准入（不会有任何在跑请求被打断）；只有显式切到 `MAX_UTILIZATION`，才会从活跃列表末尾反向找最近启动者来让位（`tensorrt-llm:cpp/tensorrt_llm/batch_manager/capacityScheduler.cpp:562`-`565`）。
- **LMDeploy**：D 走 `_schedule_prefill()`，若资源不够，`_PrefillAdmissionAttempt` 先尝试驱逐 `hanging`/`waiting` 里的候选（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:427`-`434`），不影响已在 RUNNING 的请求；只有在 `_schedule_decoding()` 续期时才可能触发对 running 请求的 `running.pop(-1)` 让位（`:829`），这条路径和 D 的准入是两个独立的调度阶段，不会同一步互相影响。
- **LightLLM**：D 的准入由 router 进程的 `ChunkedPrefillQueue._can_add_new_req` 决定——如果最坏情况模拟显示放不下，D 这一步直接不被纳入 `can_run_list`，继续留在 `waiting_req_list` 里等下一轮（`lightllm:lightllm/server/router/req_queue/chunked_prefill/impl.py:24`-`51`），不会有任何已在跑请求被 pause 来给 D 让路——D 的准入和已在跑请求的 pause 是两个独立触发条件，互不联动。
- **llama.cpp**：D 对应一个新任务，`get_available_slot()` 找不到空闲/相似 slot 就直接 `defer` 回 `queue_tasks_deferred`（`llama.cpp:tools/server/server-context.cpp:2337`），等下一轮 `update_slots()` 循环再试，已在跑的 slot 完全不受影响。
- **TGI**：D 进入 `next_batch()` 的候选，`block_allocator.allocate()` 失败就把它 `push_front` 退回队列（`tgi:backends/v3/src/queue.rs:326`），已有的 cached batch 继续跑，不发生任何请求级别的中断。
- **Tokasaurus**：D 先进 `queued_seqs`；如果 `allocate_tokens_for_decode_bumping_seqs_if_necessary()` 判断当前 decode 请求的续期都撑不住了，会从 `running_seqs` 末尾 `pop()`（`tokasaurus:tokasaurus/manager/manager.py:764`）触发 bump——这条路径是为了保住已在跑的 decode 请求，不是专门为 D 让路；D 本身能不能被排上，还要看 `greedy_prefill`/`calc_prefill_per_forward` 的整体预算决定。

**这个走查暴露出一条本篇前面分散讨论、但没有集中强调的规律**：在允许抢占的四家引擎里，vLLM 和 SGLang 的抢占/让位逻辑直接嵌在"准入新请求"这同一次调度调用里（D 的准入失败会立即触发对其他请求的让位）；LMDeploy 和 Tokasaurus 的让位逻辑则挂在**续期**（已在跑请求要求更多 token/block 时）而不是**准入**新请求的路径上——D 能不能挤进来，和已在跑请求会不会被 bump/evict，是两个时间点不完全重合的判断。这条差异在 `## 2`/`## 5` 的表格里因为按维度拆开而不容易看出来，只有把同一个场景走一遍才会浮现。

## 5. 设计决策与代价

### 决策 1：排序要不要读前缀缓存——FCFS vs LPM，以及 LPM 会不会饿死请求

- **为什么这么设计**：FCFS（vLLM/LMDeploy/LightLLM/TGI/Tokasaurus 默认）的理由是可预测性——运营者能对"一个请求最多等多久"给出上界估计，实现也最简单（`deque`/`VecDeque`/list 直接够用）。SGLang 的 LPM（`sglang:python/sglang/srt/managers/schedule_policy.py:381`-`391`，`_sort_by_longest_prefix`，排序键是 `-num_matched_prefix_tokens`）反过来押注"命中共享前缀的收益，大于打乱到达顺序的代价"——这在大量请求共享长 system prompt/few-shot 模板的场景下能显著减少前缀 block 被中间请求挤出 LRU 的概率。
- **不这样会怎样**：FCFS 场景下，如果客户端把共享前缀的请求打散发送，批次内几乎没有前缀复用，前缀缓存退化成"每请求独立 KV cache"；LPM 场景下，一个前缀完全不匹配（`num_matched_prefix_tokens=0`）的请求会被排到队尾——**本篇核实过 SGLang 源码，`schedule_policy.py` 通篇没有基于等待时长的加权或老化（aging）机制**，唯一的兜底是等待队列长度超过 128 时自动退化回 FCFS（`sglang:python/sglang/srt/managers/schedule_policy.py:290`-`294`，函数 `_determine_active_policy`），但这条判据的注释写的是"关掉昂贵的前缀匹配和排序计算"（性能考虑），公平性只是这条性能优化的副作用，不能当成显式防饥饿设计来依赖。跨引擎看，**TensorRT-LLM 的 `enablePrefixAwareScheduling`（默认开）不会有这个问题**，因为它只做"这个请求现在跳过是否划算"的二元判断（`tensorrt-llm:cpp/tensorrt_llm/batch_manager/capacityScheduler.cpp:544`-`551`），不重排整个队列，一个请求最多被跳过若干次而不会被无限期挤到队尾——这是两种"前缀感知"设计在饥饿风险上的实质差异，不是名字接近就等价。
- **什么时候可以不这样**：等待队列很短、或请求间本来就没有前缀重叠时，LPM 相对 FCFS 几乎不产生收益（`## 5` 只讲机制因果，不给具体数字）；`tree_cache.disable=True`（未启用前缀缓存）时 SGLang 会自动把 LPM 降级为 FCFS（`sglang:python/sglang/srt/managers/schedule_policy.py:311` 附近）。反过来，如果流量里长尾请求（长时间等不到共享前缀命中）比例不可忽视，运营者应该考虑改用 vLLM 式的 `PRIORITY` 二级排序（`priority` + `arrival_time` 兜底）而不是 LPM——`PRIORITY` 策略结构性地保证了"到达时间"永远是排序的一个维度，不会出现"某个特征值一直不利就永远排不上"的情况。

### 决策 2：允许抢占 vs 架构性不允许——这是比"选谁被抢占"更上游的一层决策

- **为什么这么设计**：允许抢占（vLLM/SGLang/LMDeploy/Tokasaurus）的理由是"准入要尽量激进，把决定权留到真正扛不住的那一刻"——这样能让 KV cache 利用率逼近上限，代价是需要一套让位机制兜底。不允许抢占（TRT-LLM 默认 `GUARANTEED_NO_EVICT`、TGI、LightLLM、llama.cpp）的理由反过来：**在准入的那一刻就用保守估计把"未来会不会撑爆"算清楚**，一旦通过准入就不再有被中途打断的风险。LightLLM 的 `ChunkedPrefillQueue._can_add_new_req`（`lightllm:lightllm/server/router/req_queue/chunked_prefill/impl.py:24`-`51`）是这条思路最直白的实现：假设所有在跑请求都跑到各自的（预测或最大）输出长度，模拟出未来任意时刻的 KV 峰值占用，`need_max_token_num < max_total_tokens` 才放行——只要这个不等式在准入时成立，理论上运行阶段就不需要再牺牲任何人。TRT-LLM 的类名 `GuaranteedNoEvictScheduler`（`tensorrt-llm:cpp/include/tensorrt_llm/executor/types.h:225`-`227`，文档字符串原话"a request, once started, will run to completion without eviction"）把这个承诺写进了名字本身。
- **不这样会怎样**：保守准入的代价是**并发度天花板更低**——LightLLM 假设最坏情况（所有请求都跑到预测/最大长度），比实际发生的情况更悲观，能同时接纳的请求数会少于"边跑边看"的激进准入策略；TRT-LLM `GUARANTEED_NO_EVICT` 同理，用 KV 更保守换取零中断。激进抢占的代价则是运行时抖动——一个后来的大请求可能把已经跑了很久的请求连本带利地打断重算（`## 5` 决策 3 展开"打断谁"）。
- **什么时候可以不这样**：延迟 SLA 极度敏感、且宁可拒绝新请求也不愿意任何已在跑请求被打断的场景，`GUARANTEED_NO_EVICT`/TGI/LightLLM/llama.cpp 这类保守路线是更安全的默认；追求 KV cache 利用率最大化、能容忍偶发重算开销的吞吐优先场景，vLLM/SGLang/LMDeploy/Tokasaurus 的激进准入 + 让位兜底更合适。TRT-LLM 把两条路线都做成了可切换的策略（`MAX_UTILIZATION` vs `GUARANTEED_NO_EVICT`），这本身就是"没有一个答案通吃所有工作点"的证据。

### 决策 3：让位受害者选谁——四个团队为什么都独立收敛到近似 LIFO

- **为什么这么设计**：vLLM（`vllm:vllm/v1/core/sched/scheduler.py:686`，`self.running.pop()`）、LMDeploy（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:829`，`running.pop(-1)`）、Tokasaurus（`tokasaurus:tokasaurus/manager/manager.py:764`，`running_seqs.pop()`）三家显式选择"牺牲最后加入 running 的请求"；TRT-LLM 的 `MAX_UTILIZATION` 从活跃列表末尾反向查找最近启动者（`tensorrt-llm:cpp/tensorrt_llm/batch_manager/capacityScheduler.cpp:562`-`565`）；LightLLM 的迭代耗尽式实现（`lightllm:lightllm/server/router/model_infer/mode_backend/base_backend.py:692`-`748`，`ready_reqs` 按插入序遍历，预算耗尽处触发 `wait_pause`）在效果上也偏向更晚加入的请求。**本库推断**这五家收敛的共同直觉是"沉没成本最小化"——最近加入 running 的请求，无论是刚开始 chunked prefill 还是刚从 waiting 转正，它已经消耗的计算相对更少，牺牲它损失的"已经白算的工作"最小；牺牲一个跑了很久的请求，等于把它已经产出的大量 decode token 对应的计算全部作废。只有 SGLang 反过来选"已生成 token 数最少者"（`sglang:python/sglang/srt/managers/schedule_batch.py:2825`-`2860` 附近，`_get_decode_retraction_order`）——这条规则表面上和"沉没成本最小化"的直觉相反，但本质是同一个目标的另一种度量：SGLang 直接量化"这个请求已经产出了多少 token"，而不是用"何时加入 running"做代理指标，在请求生成速度不均匀（比如有的请求刚加入就秒出很多 token）时，SGLang 的度量更精确，代价是需要额外维护一个按生成量排序的结构，不像"pop 列表末尾"那样零成本。
- **不这样会怎样**：如果反过来选"牺牲资历最老的请求"（没有引擎这么做），会把已经投入最多计算的请求作废，浪费最大，且该请求可能因为反复排在队伍前面而反复被优先执行、反复被打断，形成恶性循环。
- **什么时候可以不这样**：新加入 running 的请求恰好是一个刚开始的巨型 chunked prefill（还没产出任何 decode token，但即将占用大量后续预算）时，"最近加入=沉没成本最小"这条经验规则会失真——打断它损失确实最小，但如果频繁出现"大请求一进来就被自己触发的资源紧张打断"，会导致这类大请求系统性地难以推进（一种隐性的饥饿）。这是本库通读源码后没有在任何一家引擎里发现专门处理的边界情形，值得作为可改进点看待（见 `## 8`）。

### 决策 4：token 预算模型——共享标量 vs 逐请求最坏情况模拟 vs 固定 slot

- **为什么这么设计**：vLLM/TGI 用**共享标量**（`token_budget`/`prefill_token_budget`，每步从满额往下扣，扣完就停），实现最简单，天然支持 chunked prefill 的"这一步预算够多少算多少"。LightLLM/Tokasaurus 用**逐请求最坏情况模拟**（`lightllm:lightllm/server/router/req_queue/chunked_prefill/impl.py:35`-`36` 的 `need_max_token_num`；`tokasaurus:tokasaurus/manager/scheduler.py:434`-`530` 的 `calc_prefill_per_forward`），把"未来会不会撑爆"提前算进当前这一步的决策里，代价是每步都要跑一遍模拟（Tokasaurus 甚至要在多个时间点上滚动模拟 KV 占用曲线）。llama.cpp 用**固定 slot**，压根不做逐 token 的预算计算，只有"这个 slot 空不空"一个二元判断。
- **不这样会怎样**：纯共享标量模型不主动预判未来，可能在某一步准入的请求，几步之后因为大家都在稳定 decode 而集体撑爆 KV cache，触发让位机制（`## 5` 决策 2/3）；最坏情况模拟模型如果估计过于悲观（比如所有请求的 `max_new_tokens` 都设得很大但实际很快就停），会系统性地低估可用并发度，浪费本可以用上的显存。
- **什么时候可以不这样**：请求的输出长度分布本身很好预测（比如固定长度的分类/打分任务）时，最坏情况模拟的悲观代价很小，共享标量和最坏情况模拟在实际效果上会趋同；输出长度高度不可预测（开放式长文生成）时，最坏情况模拟的保守代价会被放大，共享标量+让位兜底反而更划算——这正好解释了为什么"要不要允许抢占"（决策 2）和"用什么预算模型"（决策 4）在各引擎里高度相关：LightLLM/Tokasaurus 选了保守预算模型，自然也倾向不依赖激进的抢占来兜底（Tokasaurus 虽然有 bump，但 `greedy_prefill=True` 的默认路径本来就很少触发它）。

### 决策 5：固定 slot（llama.cpp）vs 动态调度（其余七家）

- **为什么这么设计**：固定 slot 把"并发数"这个决策提前到启动时做一次性决定，运行时不再需要任何跨请求的资源仲裁逻辑——`get_available_slot()` 的复杂度只来自"选哪个空 slot"，不涉及"要不要为了新请求牺牲旧请求"。这是一种用**部署时的确定性**换**运行时的简单性**的设计：管理员知道这台机器最多同时服务 N 个请求，不会有任何请求因为资源竞争被打断或拒绝额外的推理开销。
- **不这样会怎样**：固定 slot 的显存利用率上限由"最坏情况下每个 slot 需要多少 KV"决定（`n_ctx / n_parallel`），如果实际请求普遍比 `n_ctx` 短很多，这部分预留显存是浪费的；动态调度按 block/page 粒度分配，能把这部分浪费降到更低，代价是需要完整的一套准入/让位逻辑（本篇 `## 2`-`## 5` 讨论的全部内容）。
- **什么时候可以不这样**：单机部署、请求长度分布已知且波动不大、更看重"行为完全可预测、没有任何隐性调度开销"的场景（比如本地开发、边缘设备、小规模自托管），固定 slot 模型的简单性本身就是优点，不需要为了榨取理论上限的显存利用率去承担动态调度的实现与运维复杂度。反过来，多租户、请求长度差异大、需要把显存利用率压到极限的生产服务场景，动态调度是几乎必然的选择——这也是为什么本篇覆盖的另外七家引擎全部走了动态调度这条路。

### 决策 6：要不要给排序策略加防饥饿兜底——本篇发现一个反直觉的分布

- **为什么这么设计**：`## 5` 决策 1 已经指出 SGLang 的 LPM **没有**基于等待时长的加权或老化机制。但本篇跨引擎复核时发现，"前缀感知排序需不需要显式防饥饿"这件事，八家引擎里唯一给出完整答案的不是 SGLang，而是 LMDeploy——它的长 prefill 重排序（`_long_prefill_policy` 默认值就是 `'size'`，不是关闭状态，`lmdeploy:lmdeploy/pytorch/envs.py:234`）会优先处理预计切块数更少的长请求，但同时叠加一个显式的"age credit"：`_long_priority_key()`（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:157`-`164`）用 `wait_age // aging_seconds_per_chunk` 算出的 `age_credit` 去抵消 `estimated_long_chunks`，等待越久，排序键越小（越优先）——**函数的 docstring 原话就是"Prefer smaller long prompts, with age credit to avoid starvation"**，`aging_seconds_per_chunk` 默认 2.0 秒（`lmdeploy:lmdeploy/pytorch/envs.py:236`）。这意味着一个长 prompt 哪怕体积再大，等待时间足够长之后也会被排到前面，理论上不存在永久饿死的可能。vLLM 的 `PRIORITY` 策略也提供了结构性保证——`arrival_time` 永远是排序键的一部分（[[03-vLLM-调度器解剖]] `## 3.3`），不存在"某个特征值一直不利就永远排不上"的情况，但这不是专门为"防饥饿"设计的，是 `PRIORITY` 排序本身自带的副产品。
- **不这样会怎样**：没有防饥饿机制的前缀感知排序（目前已知只有 SGLang 的 LPM 属于这一类），在前缀命中率长期偏向某一小撮请求特征的极端流量下，理论上会让不具备这些特征的请求持续排在队尾——`## 5` 决策 1 与 `## 7` 已详细讨论这条代价，不再重复。
- **什么时候可以不这样**：等待队列长度本来就有硬上限（SGLang 超过 128 自动退化 FCFS，`sglang:python/sglang/srt/managers/schedule_policy.py:290`-`294`）、或者部署方能保证流量里各类前缀分布足够均匀时，防饥饿机制带来的收益有限，不加也不会出大问题——这也是本库认为 SGLang"目前没做但没出大事"的原因（`## 8` 会给出这条可改进点的具体建议）。反过来，多租户场景里如果不同租户的 system prompt 完全不同（天然分成几个互不共享前缀的簇），显式防饥饿机制的必要性会显著上升，LMDeploy 的 age-credit 公式是本篇里现成能抄的参考实现。

### 决策 7：批类型要不要做成显式状态机——`ForwardMode` 枚举 vs 隐式逐 token 追赶

- **为什么这么设计**：`## 4.1` 已经列出阵营，这里补上"为什么两种做法都合理"的三样分析。SGLang 把"这一步在做什么"物化成一个枚举字段 `ForwardMode`（`EXTEND`/`DECODE`/`MIXED`/`IDLE`/…，`sglang:python/sglang/srt/model_executor/forward_batch_info.py:100` 附近），调度器的每一次决策产出都带着这个标签——好处是下游（attention 后端选核、CUDA Graph 是否可以重放）只需要读一个字段就知道这一步的物理形状，不需要重新推导。vLLM 反过来完全不设这个字段，`## 0` 引用过的源码注释原话是"There's no 'decoding phase' nor 'prefill phase' in the scheduler"（`vllm:vllm/v1/core/sched/scheduler.py:487`），每个请求只维护 `num_computed_tokens`/`num_tokens_with_spec` 两个数字，调度器和下游都靠"逐请求追赶"这条统一逻辑处理，不需要一个额外的批级别标签。
- **不这样会怎样**：显式状态机（SGLang）的代价是需要为每一种物理形状单独写分支或验证（`## 4.1` 提到 `MIXED` 是打了专门补丁才支持的第三态，不是 `EXTEND`/`DECODE` 之间的自动插值）；隐式追赶（vLLM）的代价是"这一步到底在干什么"这个问题，下游必须重新从 `num_computed_tokens` 和请求长度反推，不能直接读一个字段——TensorRT-LLM 的两级调度器（`CapacityScheduler`+`MicroBatchScheduler`，决策 8 展开）某种程度上是两种做法的折中：不维护单一枚举，但显式拆分 context/generation 两级判断。
- **什么时候可以不这样**：如果下游执行层（CUDA Graph 捕获、投机解码验证）需要频繁按"这一步是不是纯 decode"这类判断做分支优化，显式状态机能省掉大量重复推导，SGLang 走这条路和它的 CUDA Graph/投机解码实现风格是一致的；如果调度器本身要支持大量细粒度的旁路功能（KV connector、结构化输出、多模态，`vllm:vllm/v1/core/sched/output.py:206`-`297` 的 14 个字段），维护一个粗粒度的批类型标签反而可能和这些细粒度状态打架，不加更省心——这也是为什么 vLLM 选择了相反的路线。

### 决策 8：两级调度器 vs 单级调度器——TensorRT-LLM 的 `CapacityScheduler`+`MicroBatchScheduler` 为什么要拆成两层

- **为什么这么设计**：本篇覆盖的另外七家引擎，"谁能进这一步"和"这一步具体怎么切块/分配 token"通常由同一个调度对象的同一次调用决定（哪怕内部分了 RUNNING/WAITING 两个循环，也是同一个 `schedule()`）。TensorRT-LLM 把这两件事拆成物理上独立的两个类——`CapacityScheduler` 只回答"哪些请求这一步该被纳入活跃集合、要不要 pause 谁"（`tensorrt-llm:cpp/tensorrt_llm/batch_manager/capacityScheduler.cpp:668`），`MicroBatchScheduler` 再在这批已确定的活跃请求里决定 context（prefill）chunk 怎么切（`tensorrt-llm:tensorrt_llm/_torch/pyexecutor/_util.py:3082`-`3094` 里两者一起被构造）。**为什么要拆**：`AGENTS.md`（文档所述）把这条流水线描述为"Scheduler → BatchManager (in-flight batching) → KV Cache Manager"的分层架构，`CapacityScheduler` 处理的是"资源账本"层面的准入/让位，`MicroBatchScheduler` 处理的是"这一步怎么切"这个更细粒度的执行细节，两者的变化频率和关注点不同——准入策略（`GUARANTEED_NO_EVICT`/`MAX_UTILIZATION`）是运维策略选择，chunk 切法（`ContextChunkingPolicy`）是性能调优选择，拆开之后两者可以独立组合而不互相纠缠代码路径。
- **不这样会怎样**：单级调度器（其余七家）的实现更紧凑，改一条策略通常只用改一个函数；但如果像 TRT-LLM 一样想让"准入策略"和"chunk 切法"能自由组合（比如 `GUARANTEED_NO_EVICT` + `EQUAL_PROGRESS` 或 `MAX_UTILIZATION` + `FIRST_COME_FIRST_SERVED` 这类交叉），单级设计需要在同一个函数体内维护更多条件分支的笛卡尔积，可读性会随策略数量增多而下降。
- **什么时候可以不这样**：调度策略的可选项本来就不多（比如 vLLM 只有 FCFS/PRIORITY 二选一），单级调度器不会遇到"策略组合爆炸"的问题，拆成两层反而增加了理解成本（要先搞清楚决策边界在哪一层）；只有当一个引擎的准入策略和执行细节调优都各自有多个正交可选项时，两级拆分的收益才会超过它的复杂度代价——这也解释了为什么本篇覆盖的引擎里，只有策略选项最丰富的 TensorRT-LLM 走了这条路。

## 6. 口径差异与本篇不能回答什么

本篇是跨八个代码库的横向对比，比单引擎解剖篇更容易掉进"表面相似、口径不同"的陷阱，以下几点是本库诚实标注的边界：

1. **"token 预算"这个词在不同引擎里不是同一个物理量**。vLLM/TGI 的预算是纯 token 计数；TRT-LLM 的准入约束本质是 KV block 可用量（`maxNumRequests` 只是请求数上限，真正的资源约束在 `kvCacheManager` 侧，本篇 `## 2` 表格为了可比性把它归类到"token 预算参数"这一列，但读者需要知道这是一次概念上的近似对齐，不是同一个数字）；LightLLM/Tokasaurus 的"预算"是滚动模拟出来的动态曲线，不是一个静态常量。逐字对比"谁的预算数值更大"没有意义。
2. **本篇不产出、也不比较任何实测吞吐/延迟数字**——这是 `_PLAN.md` 的红线，也是本篇任务书的硬性要求。凡是"哪种策略更快"的问题，本篇只回答机制上的因果链条（比如"LPM 命中前缀能减少重复计算"），不回答"快多少"，因为这个数字依赖硬件、模型、流量形状，脱离这些前提给数字就是编造。
3. **本篇没有覆盖 LMDeploy 的 TurboMind（C++）后端的完整调度逻辑**。TurboMind 的准入/驱逐是基于时间戳的 LRU cutoff 迭代器（`lmdeploy:src/turbomind/engine/scheduler.cc:73`-`77` 一带的 `EvictingIterator`），和本篇主表所有条目的"请求级排序+让位"模型不是同一套抽象，本库判断需要独立成篇才能讲清楚，此处只在 `## 2`/`## 7` 提及其存在，不展开机制细节。
4. **TensorRT-LLM 的 `ContextChunkingPolicy` 默认解析路径未完全追踪**——`context_chunking_policy` 字段默认是 `None`（`tensorrt-llm:tensorrt_llm/llmapi/llm_args.py:3494`-`3495`），C++ 侧在未显式设置时具体走哪条分支，本库没有继续深挖调用链条确认，`## 2` 表格里"首值 FIRST_COME_FIRST_SERVED"只是枚举声明顺序，不代表已核实为运行时默认值——这一点明确标注为**未查证**，不拿枚举声明顺序冒充确认过的默认行为。
5. **多个配置项组合后的交互效应超出本篇范围**。比如 vLLM 的 `PRIORITY` 策略叠加自定义 `priority` 与前缀缓存命中率会怎么互相影响、SGLang 的 `enable_mixed_chunk` 打开后与 LPM 排序同时生效会是什么效果——本篇只逐个讲清楚单一机制，没有做组合场景的推演，需要哪种组合就去读对应引擎的解剖篇（[[03-vLLM-调度器解剖]]/[[02-SGLang-Scheduler事件循环]]）。
6. **本篇的证据全部来自静态代码走读，不包含任何运行时观测**。所有"victim 选择偏向新请求"这类结论，是读代码得出的必然逻辑推导（比如 `list.pop()` 的语义是确定的），不是靠插桩或 profiling 验证过的运行时行为——这两者在本库的证据等级体系里都算"源码为证"，但读者不应把它和"我们跑过测试观察到"混为一谈。
7. **本篇没有考虑张量/流水线并行度对调度粒度的影响**。所有讨论都隐含"单个调度决策对应单个 GPU 上的一次前向"这个简化前提；实际部署里 TP/PP/DP 会让"一步调度"和"一次 GPU 前向"不再是一一对应关系（比如 vLLM/SGLang 的流水线并行需要跨 microbatch 协调，TRT-LLM 的 MPI 多进程 worker 之间也有额外的同步开销）——这些内容属于各引擎"分布式与并行策略"篇的范围，本篇只在单实例调度决策这个层面做比较。
8. **`## 2.2` 的体量数字是本库 `wc -l` 实测的一次性快照，会随上游代码演进而变化**。八个仓库都是活跃开发中的开源项目，本篇取证基准锁定在各自 `_lab/out/repo_stats.json` 记录的 sha，几个月后重新统计这些数字大概率会有出入——这不代表本篇的结论过时，因为本篇的核心论点是"多出来的行数对应多出来的功能"这条**因果关系**，不是某个具体数字本身，因果关系比具体行数更稳定。
9. **八家引擎的"活跃程度"本身不在同一水平线上，这条差异会系统性地影响读者该怎么权衡本篇的发现**。`_lab/out/repo_stats.json` 记录的取证 sha 对应的最近提交时间：

   | 引擎 | 最近提交时间（`_lab/out/repo_stats.json`） | 距取证日（2026-08-22）多久 |
   |---|---|---|
   | vLLM / SGLang / TensorRT-LLM / LMDeploy / LightLLM / llama.cpp | 均在 2026-08-21～22 | 1 天以内 |
   | TGI | 2026-03-21 | 约 5 个月 |
   | Tokasaurus | 2025-08-19 | 约 1 年 |

   **Tokasaurus 的取证快照距今已一年**，TGI 也有约五个月的间隔——这不代表这两个引擎"过时"或"不值得参考"（Tokasaurus 的 `calc_prefill_per_forward` 滚动模拟思路依然是本篇里独一份的设计），但读者应该知道：本篇对 Tokasaurus/TGI 的机制描述是"一年前/五个月前那个版本"的快照，而对其余六家是"几乎实时"的快照——如果这两个项目在本篇写成之后有过调度相关的重构，本篇的行号引用会先失效，且失效概率明显高于其它六家。Tokasaurus 的仓库归属组织是 `ScalingIntelligence`（`_lab/out/repo_stats.json` 里 `remote` 字段记录的 clone 地址是 `github.com/ScalingIntelligence/tokasaurus.git`），一年无新提交的活跃度更符合"研究原型"的画像，不是对标 vLLM/SGLang 那样持续迭代的生产级项目，这个背景也是理解"为什么它的调度器只有 684 行却设计得相当有独创性"的一部分语境——研究原型更容易在单点设计上做深，不需要背负生产级项目必须兼顾的全部旁路功能（`## 2.2` 已经论证过"多出来的行数在做什么"这条规律）。

10. **本篇和 `04-横向对比/` 目录下其余五篇的边界要划清楚，避免读者以为本篇什么都讲了**：KV cache 分配算法本身、前缀缓存的树/哈希结构实现细节，属于该目录下专讲"KV 缓存与前缀复用"的横向对比篇，本篇只在"调度器怎么读/怎么用"这个边界内讨论前缀缓存（比如 LPM 怎么查树、TGI 的 block allocator 怎么返回 `prefix_len`），不展开树结构本身怎么实现；八家引擎的 API 兼容性、OpenAI 协议覆盖度，属于该目录下专讲"API 兼容性"的横向对比篇；工程规模与代码结构的完整对照（不只是调度器这一个子系统），属于"工程规模与代码结构对比"篇，本篇 `## 2.2` 只是这个更大话题在调度器这一个子系统上的局部样本；选型建议（"我该用哪个引擎"）属于"选型决策树"篇，本篇只提供机制层面的因果链条，不给"哪个更好"的结论；性能数字的口径与陷阱，属于"性能口径与基准陷阱"篇，本篇通篇不产出任何性能数字正是为了不和那篇的职责重叠。

## 7. 踩坑与反直觉

1. **LMDeploy 的 `swap_in_map`/`swap_out_map` 是贯穿三层调用链的死代码**。`SchedulerOutput`（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:66`-`73`）声明了这两个字段，一路透传到 `InputsMaker`（`lmdeploy:lmdeploy/pytorch/engine/inputs_maker.py:263`-`264`）、再到 `cache_engine.swap_in`/`swap_out`（`lmdeploy:lmdeploy/pytorch/engine/model_agent/agent.py:157`-`161`）——整套 swap-to-host 的管线都还在，但 `_schedule_prefill`/`_schedule_decoding` 里这两个字典自始至终只被初始化为空 `dict()`（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:738`-`739`、`:799`-`800`），本库通读全部调度代码没有发现任何一处对它们赋值。佐证是 `eviction_helper` 目录下**只有一个实现**（`recompute_eviction_helper.py`），`eviction_type` 配置默认值也是 `'recompute'`（`lmdeploy:lmdeploy/pytorch/config.py:102`）——这套 swap 管线更像是历史遗留或为将来预留的接口，不是当前生效的代码路径。
2. **TensorRT-LLM 的默认策略根本不会 pause 任何请求**。`GUARANTEED_NO_EVICT` 是默认值（`tensorrt-llm:tensorrt_llm/llmapi/llm_args.py:3490`-`3491`），只有显式切到 `MAX_UTILIZATION` 才会触发 `## 5` 决策 3 讨论的"反向查找最近启动者"逻辑。第一次读 TRT-LLM 调度代码容易把 `capacityScheduler.cpp` 里的 pause 逻辑当成默认行为来理解，但实际上开箱即用的 TRT-LLM 服务在默认配置下和 TGI/LightLLM 一样是"零请求级中断"的。
3. **TGI 压根没有请求级抢占，"evict" 这个词在它的代码里只指前缀缓存 block 的驱逐**。`tgi:backends/v3/src/radix.rs:363`（`pub fn evict`）操作的对象是 radix trie 里未被任何在跑请求引用的缓存 block，和"牺牲一个正在生成的请求"完全是两回事；请求级别的资源冲突处理方式是简单地把请求 `push_front` 退回队列（`tgi:backends/v3/src/queue.rs:301`/`:326`/`:357`/`:371` 反复出现同一模式），已在跑的批次从未被打断。
4. **llama.cpp 并不是"完全无脑轮询"的固定 slot——它默认就有前缀感知的 slot 选择**。`slot_prompt_similarity` 默认值是 `0.1`（`llama.cpp:common/common.h:678`），不是 `0.0`（关闭），意味着开箱即用的 llama.cpp server 会优先把请求路由到"最长公共前缀相似度最高"的 slot（`llama.cpp:tools/server/server-context.cpp:1514`-`1548`），只有相似度都低于阈值才退化到 LRU 选择。这条机制的效果和 SGLang 的 LPM 神似（都是"利用前缀相似度做放置决策"），但作用域完全不同——llama.cpp 只是在几个固定 slot 之间选一个，不是对整个等待队列重排序，本库认为把两者简单类比为"都支持 LPM"是错误的。
5. **Tokasaurus 的默认行为其实是"prefill 优先"，和它 684 行的"极简"名声不太符合直觉**。`greedy_prefill` 默认 `True`（`tokasaurus:tokasaurus/common_types.py:109`），这条路径下 `schedule()`（`tokasaurus:tokasaurus/manager/scheduler.py:576`）遇到非空 `queue.queued_seqs` 就直接 `make_decision(float("inf"))`（尽快榨干所有排队/在跑的 prefill），效果上接近 SGLang 的"prefill 严格优先"。684 行文件里真正独特的部分——`calc_prefill_per_forward` 的 KV 占用曲线模拟——只有在 `greedy_prefill=False` 时才会被调用到，这是本篇任务书专门点名"这个体量差是好抓手"的地方，但读者需要知道：**默认配置下，这份"好抓手"代码其实没有被执行**。
6. **SGLang 的 LPM 没有防饥饿机制，这条已经被 [[02-SGLang-Scheduler事件循环]] 核实过，本篇跨引擎复核后要补一句更完整的对比**：TRT-LLM 的 `enablePrefixAwareScheduling` 默认开，但它做的是"跳过是否划算"的二元判断而非重排序，所以不存在"排到队尾出不来"的风险；llama.cpp 的前缀相似度只影响 slot 选择，同样不存在请求被无限期挤到队尾的问题。**在本篇覆盖的三种"前缀感知调度"里，只有 SGLang 的 LPM 有理论上的饥饿风险**，这是 SGLang 用"重排整个队列"换取更高前缀命中率所付出的、目前源码里没有兜底的代价。
7. **想到"防饥饿"这件事的不是名气最大的 SGLang，是知名度低得多的 LMDeploy**。多数读者对"哪个引擎更懂前缀缓存/更懂公平性"的第一直觉会指向 SGLang（毕竟 RadixAttention 是它的招牌），但本篇核实源码后发现：真正带着"avoid starvation" docstring、并且默认开启（`lmdeploy:lmdeploy/pytorch/envs.py:234` 的 `opt_ttft_policy` 默认值就是 `'size'`）的 age-credit 防饥饿实现，出自 LMDeploy 的长 prefill 重排序逻辑（`lmdeploy:lmdeploy/pytorch/paging/scheduler.py:157`-`164`），SGLang 的 LPM 反而是三家"前缀感知"引擎里唯一没做这件事的。名气和某个具体设计细节的完成度不是一回事。
8. **LightLLM 是本篇里唯一把"准入"和"续跑"拆到两个不同 OS 进程的引擎**（`## 1` 已详细展开）。第一次读 LightLLM 代码容易假设它和 vLLM/SGLang 一样，调度决策集中在一个对象/一次调用里——实际上 `ChunkedPrefillQueue.generate_new_batch()` 跑在 router 进程，`base_backend.py` 的 prefill/decode 分类与 pause 触发跑在独立的模型 TP worker 进程（`lightllm:lightllm/server/router/model_infer/model_rpc.py:208` 的 `mp.Process`），两者之间只通过共享内存/序列化的请求对象通信，不是一次函数调用能读全的。
9. **TensorRT-LLM 用 `std::list` 而不是 `std::vector` 存活跃请求，这个选择和它的让位算法是自洽的，不是随意选的容器**。`## 3.3` 已经指出 `RequestList = std::list<std::shared_ptr<LlmRequest>>`（`tensorrt-llm:cpp/include/tensorrt_llm/batch_manager/common.h:36`），而 `## 5` 决策 3 讲的 `MAX_UTILIZATION` pause 逻辑恰好是"反向遍历找最近启动者、找到就地删除"——双向链表的这两个操作都是 O(1)，如果换成 `std::vector`，从中间删除元素需要搬移后续所有元素，是 O(n)。第一次读代码容易觉得"用链表存请求列表"是性能反模式（缓存不友好），但结合让位算法的访问模式看，这个选择是经过权衡的，不是疏忽。
10. **Tokasaurus 的 `schedule()` 函数（`## 2` 主表已引用为主入口）docstring 开头是一句非正式的注释**（"Bro trust me, it's a good scheduler. I sketched it out on my iPad bro."，`tokasaurus:tokasaurus/manager/scheduler.py:588`），和本篇覆盖的其它引擎（尤其是 TensorRT-LLM 大量带正式 docstring 的 C++ 头文件、SGLang 的中文/英文夹杂的详细注释）文档风格反差很大——这不影响本篇对它的机制分析（`calc_prefill_per_forward` 的 KV 占用曲线模拟本身经得起推敲，`## 4` 已验证），但提醒读者：**文档的正式程度和代码的正确性/精巧程度不是同一回事**，Tokasaurus 是本篇里"文档最随性、算法却最有独创性（`calc_prefill_per_forward` 是八家里唯一做滚动模拟的）"的反差样本。

## 8. 可改进点

以下均为本库基于跨引擎源码对比给出的推断，标注证据依据；未核实是否已有对应引擎的官方 issue 在跟踪。

1. **保守准入路线（TGI/LightLLM/TRT-LLM `GUARANTEED_NO_EVICT`/llama.cpp）在预测失准时缺少兜底**。LightLLM 的最坏情况模拟依赖 `ema_req_out_len` 这类预测值（`lightllm:lightllm/server/router/req_queue/chunked_prefill/impl.py:15`），如果实际输出长度系统性偏离预测（比如模型突然开始生成异常长的重复内容），准入时的"安全"判断可能变得不再安全；TGI/llama.cpp/`GUARANTEED_NO_EVICT` 则完全依赖固定预算/固定 slot，对这类分布漂移没有任何自适应能力。可以考虑借鉴 SGLang `new_token_ratio` 的自适应校准思路（`sglang:python/sglang/srt/managers/scheduler_components/new_token_ratio_tracker.py:14`）：观测到实际输出长度与预测持续偏离时，动态收紧准入阈值，而不是依赖一次性算好的静态预算。风险：这类自适应机制本身可能引入新的调参负担和震荡风险，需要用真实流量验证收敛性（本库不产出这类实测数字）。
2. **让位受害者选择普遍缺少"大请求刚加入即被打断"的特殊处理**（`## 5` 决策 3 提到的边界情形）。vLLM/LMDeploy/Tokasaurus 的 LIFO 式选择，在"新加入的是一个尚未产出任何 token 的巨型 chunked prefill 请求"场景下，可能反复打断同一类请求。可以考虑给"投入的 sunk cost"加一个更精确的度量（比如已计算 token 数，而不是简单的加入时间先后），SGLang 的 `_get_decode_retraction_order` 已经这么做了（虽然只覆盖 decode 阶段），vLLM/LMDeploy/Tokasaurus 可以评估类似思路是否值得引入。风险：更精确的度量需要额外维护排序结构，牺牲当前"pop 列表末尾"零成本的实现简洁性。
3. **跨引擎术语不统一本身是一种可改进的生态问题**（本库推断，非任何单引擎的代码缺陷）。`## 3.1` 列出的八个名字（preempt/retract/pause/evict/bump/…）指向两三种真实语义，但没有统一词汇表——运维团队从一个引擎切换到另一个引擎时，容易因为望文生义而误判行为（比如把 TGI 的 `evict` 当成请求级抢占）。这不是任何单个开源项目能单方面解决的问题，但社区如果能就"连续批处理"相关的核心术语（排序/让位/预算/chunking）达成一份跨项目对照表，能显著降低这类误判——本篇 `## 3.1`/`## 2` 的两张表本质上就是在尝试做这件事。
4. **llama.cpp 的 `slot_prompt_similarity` 只在"选 slot"层面利用前缀信息，没有反过来影响准入顺序**。`get_available_slot()` 目前的逻辑是"有相似 slot 就用，没有就 LRU"（`llama.cpp:tools/server/server-context.cpp:1500`-`1567`），不会因为"某个排队中的请求即将有一个高相似度 slot 空出来"而调整处理顺序。理论上可以让 `defer` 回队列的任务在下次调度时优先匹配到刚变空的高相似度 slot，但这需要给"任务-slot 亲和性"加一层持久化的排队优先级，目前的实现里没有这个结构（本库推断，未核实是否已有上游讨论）。
5. **SGLang 的 LPM 可以几乎原样借用 LMDeploy 的 age-credit 公式**（呼应 `## 5` 决策 6、`## 7` 反直觉 7）。`_sort_by_longest_prefix()`（`sglang:python/sglang/srt/managers/schedule_policy.py:381`-`391`）目前排序键只有 `-num_matched_prefix_tokens` 一项，可以参照 LMDeploy `_long_priority_key()` 的思路，把 `(等待时长 // 常数)` 算出的 age credit 叠加进排序键，让长期得不到前缀命中的请求随等待时间递减惩罚——两个引擎的场景高度相似（都是"按某种匹配度给请求打分排序"），公式几乎可以直接迁移，只是要重新调校 `aging_seconds_per_chunk` 这类常数以匹配 SGLang 自己的 batch 节奏。风险：加入等待时长会引入新的调参维度，且需要验证不会显著拉低 LPM 本来想最大化的前缀命中率——这是一个需要用真实流量做权衡的问题，本库不产出这类实测数字。
6. **保守准入路线普遍没有把"让位机制"当成可选的兜底逃生舱**（呼应 `## 5` 决策 2）。TGI/LightLLM/llama.cpp 架构性地不支持任何形式的请求级让位，`GUARANTEED_NO_EVICT` 也只是"当前策略选择"而非"架构上做不到"——TRT-LLM 自己就证明了同一套代码库可以同时支持保守（`GUARANTEED_NO_EVICT`）和激进（`MAX_UTILIZATION`）两种模式。对 TGI/LightLLM 而言，即便默认保守准入，也可以考虑加一条"仅在极端罕见情况下触发"的让位兜底（类似断路器模式），而不是把"零中断"焊死成架构约束——这样能在准入模型估计失准的极端情况下，用一次可控的让位代价换取避免更糟的结果（比如整个服务因为无法准入新请求而排队雪崩）。风险：一旦允许让位，就要重新引入 `## 3.1` 讨论的"选谁/放哪儿"这一整套决策，这正是保守路线最初想避免的复杂度，需要谨慎评估这个"逃生舱"多久触发一次、值不值得为此增加代码复杂度。

## 9. 自测题与延伸阅读

**闭卷自测题**（合上本文，尝试不看源码回答）：

1. SGLang 的 `schedule_policy` 默认值是什么？"LPM 是 SGLang 默认策略"这个常见印象为什么是错的？LPM 有没有防饥饿机制，兜底阈值解决的是公平性问题还是性能问题？
2. 八家引擎里，哪几家默认允许"牺牲一个正在跑的请求"腾资源？哪几家架构性地不允许？TensorRT-LLM 属于哪一类，取决于哪个配置项？
3. vLLM、LMDeploy、Tokasaurus、TRT-LLM（`MAX_UTILIZATION`）在选择让位受害者时有什么共同倾向？只有哪一家反过来选"已生成 token 数最少者"？本篇给出的机制解释是什么？
4. TGI 代码里的 `evict()` 函数操作的对象是什么？它和"抢占一个正在生成的请求"是不是同一回事？
5. llama.cpp 默认是否会做前缀感知的请求路由？这个机制作用于"整个等待队列的排序"还是"几个固定 slot 之间的选择"？和 SGLang 的 LPM 是不是同一类机制？
6. LMDeploy 的 `SchedulerOutput` 里声明了 `swap_in_map`/`swap_out_map` 字段，这两个字段在当前调度代码里实际被赋值过吗？佐证证据是什么？
7. Tokasaurus 的 `greedy_prefill` 默认是开还是关？这个默认值下，`calc_prefill_per_forward` 的 KV 占用曲线模拟逻辑会不会被执行到？
8. 固定 slot（llama.cpp）和动态调度（其余七家）在显存管理粒度上的本质区别是什么？各自更适合什么部署场景？
9. 连续批处理相对静态批处理的收益，依赖哪两个同时成立的前提条件？这条判据对本篇覆盖的八家引擎是否都成立？
10. 在本篇覆盖的三种"前缀感知调度"（SGLang LPM、TensorRT-LLM `enablePrefixAwareScheduling`、llama.cpp `slot_prompt_similarity`）里，哪一种存在理论上的饥饿风险？哪一家引擎其实默认就带着显式的防饥饿"age credit"机制，且不是通常被认为最懂前缀缓存的那一家？
11. LightLLM 的"准入"决策和"续跑/pause"决策分别跑在哪个进程里？这和 vLLM/SGLang 把两者放在同一次调度调用里相比，多出了什么潜在的一致性风险？
12. `## 2.2` 里，llama.cpp 的调度相关文件有 5,475 行，Tokasaurus 任务书给出的"684 行"分别掉进了什么陷阱？拿"文件行数"当"调度逻辑复杂度"的代理指标为什么不可靠？
13. TensorRT-LLM 为什么用 `std::list` 而不是 `std::vector` 存活跃请求列表？这个选择和它的 `MAX_UTILIZATION` 让位算法有什么关系？
14. 场景：KV cache 基本占满，新请求 D 到达且没有任何在跑请求主动结束。八家引擎里，哪几家的"让位"逻辑会因为 D 的准入失败而立即被触发？哪几家的让位逻辑挂在续期路径上、和 D 的准入是两个不完全同步的判断？
15. `## 0` 的三轴定位表里，哪一条轴上没有任何一家引擎选择"完全不做前缀感知"？（提示：即便是 TGI/LightLLM 这类 FCFS 阵营，前缀缓存本身是否默认开启，和"排序时看不看前缀命中率"是两回事。）
16. TensorRT-LLM 的 `CapacityScheduler` 和 `MicroBatchScheduler` 分别回答什么问题？为什么本篇覆盖的其余七家引擎大多没有这种两级拆分？

**延伸阅读（本库内，仅从 `_PLAN.md` §6 名册选取）**：

- [[03-vLLM-调度器解剖]] —— vLLM `Scheduler.schedule()` 的完整走读，本篇大量决策与引用的一手来源
- [[02-SGLang-Scheduler事件循环]] —— SGLang 三进程模型、LPM/retract 的完整机制细节，本篇 `## 5` 决策 1/3 的一手来源
- [[01-TensorRT-LLM]] —— TensorRT-LLM C++ 调度器下沉、`CapacitySchedulerPolicy` 三种策略的完整讨论

---

读完本篇应该能回答的一句话总结：**"连续批处理"不是一个统一的算法，是同一个三问题（怎么排序、资源不够牺牲谁、这一步谁吃多少 token）的八份不同答卷**——FCFS 是默认多数派、前缀感知排序是少数派、允许抢占与否是一条比"选谁被抢占"更上游的架构分水岭，而四个互不知情的团队在"选谁被抢占"这个子问题上独立收敛到近似 LIFO，这比任何单一引擎的设计选择本身更能说明"沉没成本最小化"是这个问题的自然解。所有这些差异只在输出长度异质、请求在线到达的流量下才产生真实后果——脱离这个前提，本篇讨论的一切机制差异都不重要。
