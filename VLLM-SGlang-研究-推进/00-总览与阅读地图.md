# 00 · 总览与阅读地图

> 这一篇是我亲笔写的，不是 agent 代写的。它负责三件事：**说清这个库的立场**、
> **给一张能直接照着走的阅读路线**、**把已经查实的硬结论摆在最前面，省得你翻 40 篇**。

---

## 1. 这个库的立场

市面上讲推理引擎的材料，绝大多数停在两种形态：

- **参数手册**：把 `--max-num-seqs` 是什么抄一遍，不告诉你调它会牺牲什么；
- **架构图 + 形容词**：一张方框图，配上"高吞吐""零拷贝""业界领先"。

两种都有一个共同毛病：**没法证伪**。你读完之后并不知道自己该信几分，
也没法验证作者到底有没有打开过那个文件。

所以这个库只做一件事：**把每一句关于代码的话，钉到一行真实源码上。**

具体落成三条机制：

1. **行号引用**。正文里凡是讲实现，都写成下面这个形状（跨引擎时前面再加 `sglang:` 之类的前缀）：

   ```
   `vllm/v1/core/sched/scheduler.py:123`
   ```

2. **`_verify.py` 逐条核对**。它会真的打开那个文件、数到那一行，越界就 FAIL。
   写这份文档时，全库 **1,161 条代码引用，100% 核过行号**。
3. **取证基准头**。每篇开头写死 `` `<engine>` @ `<sha>` ``，与 `_lab` 记录的 clone sha
   不一致直接 FAIL —— 防止"拿 A 版本的代码讲 B 版本的行号"。

再加一条内容上的红线：**每个"它很好"后面必须跟"什么时候这是错的"**。
只夸不说代价的段落一律回炉。

还有一条能力边界，写在最前面免得你误会：**本机没有 GPU，本库不产出任何实测吞吐/延迟数字。**
凡是涉及性能的量化，要么是带公式和假设的解析计算（会明确标注），
要么是引上游文档并注明出处文件与完整口径（硬件/模型/长度分布/并发）。
给不全口径的数字，本库宁可不写。

---

## 2. 和已有库的边界

这个仓库里已经有几个相邻的库，别读串了：

| 已有库 | 讲什么 | 和本库的分工 |
|---|---|---|
| `vLLM解剖-高吞吐推理系统/` | 精读 Aleksa Gordić 那篇 44 页长文（2025-08 的 vLLM） | 那是**读一篇文章**；本库是**读源码**，且横跨 12 个引擎 |
| `attention-optimization/` | FlashAttention / PagedAttention 的**数学与算子** | 那讲"算子内部为什么快"；本库讲"引擎怎么编排与选择算子" |
| `llm-inference/` | 各推理技术的概念笔记 | 那是概念索引；本库是带行号的源码级解剖 |

一句话分工：**那边讲原理，这边讲这份代码里原理长成了什么样、代价是什么、哪里还能改。**

---

## 3. 取证基准（12 个引擎的快照）

所有结论都基于下面这些 commit。要复现，clone 到同一个 sha 即可（命令见 [[_PLAN]] §2）。

| 引擎 | commit | 快照日期 | 总行数 | Python 产品码 | CUDA 文件 | Triton 文件 | Rust 行 | C/C++ 行 |
|---|---|---|---:|---:|---:|---:|---:|---:|
| TensorRT-LLM | `75b023cd` | 2026-08-22 | 2,452,198 | 792,216 | 429 | 91 | 0 | 598,382 |
| SGLang | `15a43983` | 2026-08-22 | 2,317,985 | 1,254,674 | 246 | 249 | 156,078 | 51,979 |
| NVIDIA Dynamo | `2a6da14c` | 2026-08-22 | 2,220,830 | 193,827 | 2 | 0 | 608,542 | 1,533 |
| vLLM | `7ca49fbe` | 2026-08-22 | 1,914,984 | 932,277 | 169 | 202 | 109,867 | 55,159 |
| llama.cpp | `2c6b141e` | 2026-08-22 | 992,377 | 58,859 | 273 | 0 | 0 | 663,024 |
| Mooncake | `3b5a5941` | 2026-08-22 | 548,895 | 30,573 | 38 | 0 | 4,405 | 420,627 |
| KTransformers | `95009ea6` | 2026-08-21 | 403,076 | 152,237 | 21 | 8 | 0 | 127,399 |
| LMDeploy | `1263d5cd` | 2026-08-21 | 339,214 | 155,986 | 130 | 34 | 0 | 67,110 |
| TGI | `b4adbf2f` | **2026-03-21** | 189,926 | 98,255 | 31 | 4 | 23,443 | 1,251 |
| LightLLM | `87881ae7` | 2026-08-21 | 148,574 | 103,709 | 0 | 129 | 0 | 0 |
| MLC-LLM | `9fa644f5` | 2026-08-17 | 100,240 | 59,516 | 0 | 0 | 0 | 22,690 |
| Tokasaurus | `72689052` | **2025-08-19** | 13,219 | 11,405 | 0 | 0 | 0 | 0 |

数字全部由 `_lab/repo_stats.py` 从真源码算出，落在 `_lab/out/repo_stats.json`。
口径：只统计登记在册的语言后缀，跳过 `.git` 与构建产物；"Python 产品码"= 路径里不含
`test` 的 `.py` 行数（含空行与注释，因为"读这个文件要翻多少屏"才是这里想表达的量）。

**表里两个日期值得单独看**：TGI 的最后一个提交停在 2026-03-21，
Tokasaurus 停在 2025-08-19 —— 快照当天它们分别已经 5 个月和 12 个月没动。
这不是本库的评价，是 `git log` 的事实。

---

## 4. 已经查实的硬结论（不用读完全库也能带走）

下面每条都能在正文里找到带行号的出处。**这些是本库目前最反直觉的部分。**

**关于规模的直觉是反的。**
SGLang（2,317,985 行 / Python 产品码 1,254,674）**比 vLLM 更大**（1,914,984 / 932,277）。
"SGLang 是那个更轻的"这个印象在这个快照上不成立。而且两家都已经不是纯 Python 项目了：
vLLM 有 109,867 行 Rust，SGLang 有 156,078 行。

**"OpenAI 兼容"是个需要逐字段核的说法。**
vLLM 与 SGLang 的 `ChatCompletionRequest` 都是 68 个字段，其中命中 OpenAI 官方字段的
只有 22 个 —— 也就是说**三分之二的字段是各家私货**，两家各自缺 8 个 OpenAI 标准字段。
拿"兼容 OpenAI"当无损替换的前提，会在这 46 个私有字段和 8 个缺口上出事。

**两家都在悄悄做 Anthropic 兼容。**
vLLM 和 SGLang 都实现了 `/v1/messages` 与 `/v1/messages/count_tokens`（Anthropic Messages API），
vLLM 还多一层 Cohere（`/cohere/v2/chat`）。llama.cpp 与 Dynamo 也有 `/v1/messages`。
"OpenAI 协议是事实标准"这句话正在被改写成"OpenAI + Anthropic 双标准"。

**旋钮数量差了一倍，但生成方式更值得看。**
SGLang 的 `ServerArgs` 有 **476 个字段**，而 `server_args.py` 里只有 **29 处 `add_argument(`** ——
CLI 是从 dataclass 自动生成的。vLLM 的 `EngineArgs` 是 **233 个字段 / 228 处显式 `add_argument`**。
自动生成让加旋钮的边际成本趋近于零，这既是 476 的成因，也是它的代价。

**"默认值不同"这件事，我自己的工具先给出了假答案。**
`compare.py` 最初报"41 个同名旋钮默认值不同"。逐条看才发现绝大多数是假的：
vLLM 的 `EngineArgs` 把默认值转交给子配置（写成 `ModelConfig.dtype`），
拿这种引用去和 SGLang 的字面量比，**必然**判成"不同"。
加了 `classify_default()` 只比字面量之后，真正可比且不同的只剩 **4 个**
（`block_size` / `enable_lora` / `enable_metrics` / `enable_prefix_caching`），37 个被判为不可比。
这条留在这里当纪念：**跨项目对比里，最容易造假数据的不是别人，是你自己的脚本。**

**vLLM 的 CUDA 路径上已经没有自己的 paged attention kernel 了。**
那个让 vLLM 出名的手写 `paged_attention_v1/v2`，在这个快照的 NVIDIA 路径上已经不在 ——
`csrc/attention/` 目录现在只剩 6 个 dtype 头文件（`attention_dtypes.h`、`dtype_fp8.cuh` 等），
kernel 本体没了，注意力全部外包给 FlashAttention / FlashInfer / Triton。
**但这句话有个必须说清的边界（我自己 grep 复核出来的）**：这个名字并没有从仓库里消失，
它还活在 ROCm 路径上 —— `csrc/rocm/attention.cu`、`csrc/rocm/ops.h`、`csrc/rocm/torch_bindings.cpp`，
以及 `vllm:vllm/v1/attention/backends/rocm_aiter_fa.py:1362` 调用的 `torch.ops.aiter.paged_attention_v1`
（那是 AMD AITER 库提供的算子，不是 vLLM 自己的）。
所以准确说法是**"NVIDIA 路径上退役了"，不是"整个项目删掉了"**。
另外 `VLLM_ATTENTION_BACKEND` 环境变量已全仓零命中，改成 `--attention-backend`。

**前缀缓存的驱逐不是真 LRU。**
vLLM 没有维护时间戳，而是靠**调用顺序**近似最近性：`BlockPool.free_blocks` 把释放的块分两路，
没缓存过的走 LIFO（为 GPU 局部性），带 hash 的走 FIFO（为缓存寿命）；
释放一个请求时还会先把它的块列表**倒序**，让尾部（最不可能被共享的）块排在前面等着被驱逐。

**SGLang 那个教科书级的 `RadixCache` 不是默认生产路径。**
`radix_cache.py` 里 863 行的 `RadixCache` 现在主要是 `HiRadixCache` / `PureSWARadixCache` 的父类，
工厂函数兜底选中的是另一个 2,894 行的 `UnifiedRadixCache`。照着教程读源码容易读错文件。

**CUDA Graph 掉回 eager 是完全静默的。**
`vllm/compilation/cuda_graph.py` 在 fallback 时直接转发给未编译的可调用对象，一句日志都不打。
cascade attention 和 encoder-decoder 的首步都会触发它。想看见，只能开 `--cudagraph-metrics`（默认关）。

**抢占选的是最新的请求，不是最老的。**
vLLM 在 FCFS 下的抢占受害者选择是 `self.running.pop()` —— LIFO。
直觉上"先来后到，该保老的"，代码上确实也是保老的：**踢掉最新的才等于保护最老的。**

**"静默吞异常"的密度是可以横向比的。**
`_lab/improve.py` 按每万行 Python 统计"except 之后既不重抛也不记日志"的处数：
vLLM 5.4、TensorRT-LLM 8.4、SGLang 9.0、LMDeploy 9.1、TGI 9.4、LightLLM 14.0、
Dynamo 21.9、KTransformers 29.2。
推理引擎里静默降级比报错危险 —— 用户看不见自己掉进了慢路径。
**但这是信号不是判决**：有些吞异常是刻意的兜底（可选依赖导入），写进正文前必须逐条看上下文。

---

## 5. 阅读路线

**路线 A · 我要搞懂一个引擎怎么跑起来的（推荐首次阅读）**

1. [[01-vLLM-全景与代码地图]] —— 先建立地形感
2. [[02-vLLM-V1架构与EngineCore循环]] —— 一个请求穿过哪些进程
3. [[03-vLLM-调度器解剖]] —— 连续批处理落到代码里是什么样
4. [[04-vLLM-KV缓存与前缀缓存]] —— 显存是怎么被切开用的
5. [[06-vLLM-模型执行与CUDA-Graph]] —— GPU 那一步到底做了什么

**路线 B · 我要在 vLLM 和 SGLang 之间选一个**

1. [[04-工程规模与代码结构对比]] —— 先看两边的工程体量与复杂度落点
2. [[01-API兼容性横向对比]] —— "OpenAI 兼容"到底兼容到什么程度
3. [[02-调度策略横向对比]] —— FCFS vs 最长前缀优先，对你的流量意味着什么
4. [[03-KV缓存与前缀复用横向对比]] —— 块哈希表 vs 基数树
5. [[05-选型决策树]] —— 收束成一张可执行的判断表

**路线 C · 我要给上游提 PR**

1. [[03-跨引擎共性缺口]] —— 哪些问题是所有引擎都没解决的
2. [[01-vLLM可改进点]] / [[02-SGLang可改进点]] —— 带 file:line 的具体位置
3. [[04-可落地贡献清单]] —— 按难度和影响排过序

**路线 D · 我只关心某个子系统**

调度 → [[03-vLLM-调度器解剖]] + [[02-SGLang-Scheduler事件循环]]
KV/前缀 → [[04-vLLM-KV缓存与前缀缓存]] + [[03-SGLang-RadixAttention与前缀缓存]] + [[04-SGLang-内存池与KV布局]]
注意力后端 → [[05-vLLM-注意力后端与算子层]] + [[05-SGLang-注意力后端矩阵]]
投机解码 → [[10-vLLM-投机解码]] + [[10-SGLang-投机解码EAGLE]]
结构化输出 → [[11-vLLM-结构化输出]] + [[06-SGLang-约束解码与语法后端]]
PD 分离 → [[12-vLLM-PD分离与KV-Connector]] + [[11-SGLang-PD分离与HiCache分层]]
分布式并行 → [[07-vLLM-分布式与并行策略]]
API 表面 → [[08-vLLM-HTTP-API表面全解]] + [[08-SGLang-HTTP-API表面全解]]
配置系统 → [[09-vLLM-Python-API与EngineArgs]] + [[09-SGLang-ServerArgs旋钮全景]]
SGLang 的前端 DSL → [[07-SGLang-前端DSL与编程模型]]

**路线 E · 我想看看 vLLM/SGLang 之外还有什么**

从 [[11-开源推理引擎谱系图]] 进，再按兴趣挑单篇：
[[01-TensorRT-LLM]]、[[02-LMDeploy与TurboMind]]、[[03-TGI]]、[[04-LightLLM]]、
[[05-llama.cpp-server]]、[[06-MLC-LLM]]、[[07-KTransformers]]、[[08-Mooncake传输引擎]]、
[[09-NVIDIA-Dynamo]]、[[10-Tokasaurus]]。

最后收在 [[99-本质总结-亲笔]]。

---

## 6. 怎么自己验一遍

这个库最该被使用的方式不是"读完信了"，而是**抽查**：

```bash
cd VLLM-SGlang-研究-推进

# 1) 全库体检：取证基准头 + 每条 file:line + 双链 + 占位符
python _verify.py

# 2) 重算所有统计数字（需要先按 _PLAN.md §2 把源码 clone 到 _src/）
cd _lab
python repo_stats.py && python api_surface.py && python struct_map.py
python compare.py && python improve.py

# 3) 工具自身的回归测试（不依赖 _src 也能跑一半）
python -m pytest tests -q
```

随便挑一篇里的任意一条行号引用，打开 `_src/` 下那个文件翻到那一行。
**如果对不上，那就是本库的 bug，不是你的理解问题。**

---

## 7. 这个库怎么续写

读 [[_PLAN]]：§4 是篇目表（⬜ 未写 / ✅ 已完成），§5 是每篇的十段式骨架，
§6 是双链名册（正文里的双链只许从那儿挑，写别的名字会被判死链），§7 是已经踩过的坑。

改完必须跑通两件事才算数：`python _verify.py` 零错误，
`cd _lab && python -m pytest tests -q` 全绿。
