# Jinja 与 Chat Template(对话模板)

> Jinja 是把"结构化对话(messages 列表)"渲染成"模型真正吃的那一串纯文本"的模板引擎,是 SFT 数据工程里连接「数据格式」与「模型输入」的关键一环。📍 导航：[[00-知识地图]]
> 🔗 相关：[[llm-train/README]] [[llm-alignment/RLHF]] [[llm-algo/transformer/模型架构]] [[llm-application/README]]

## 阅读地图

| 你想知道 | 看哪一节 |
|---|---|
| Jinja 是什么、为什么 SFT 要用它 | 0、1 |
| messages 列表长什么样 | 2 |
| 模板把对话渲染成什么样的字符串 | 3 |
| 模板引擎内部怎么工作(渲染流程) | 4 |
| 特殊 token、BOS/EOS、role 标记 | 5 |
| `apply_chat_template` 的关键参数 | 6 |
| 训练 vs 推理两种用法的区别 | 7 |
| 怎么自己写/调试一个模板 | 8、配置示例 |
| 容易踩的坑 | 常见问题 |

## 0. 一句话锚点

**Jinja 模板 = 一段"带占位符和循环的文本",输入是 `messages`(一个个 `{role, content}` 字典),输出是一整串带特殊标记的纯文本,这串文本才是 tokenizer 真正去 encode、模型真正去训练/推理的对象。**

换句话说:你在数据集里看到的是结构化 JSON,模型看到的是被模板"拍平"后的一维字符串。**这两者之间的转换规则,就是 chat template,而它通常用 Jinja2 语法写成。**

## 1. 地基:它解决什么问题

### 1.1 问题的来源

大语言模型本质上是"输入一串 token,预测下一个 token"。它**不认识** `role`、`content` 这种结构,只认识连续的 token 序列。

但人类对话天然是结构化的:有"谁说的(role)"、"说了什么(content)"、"轮次(turn)"。于是产生一个落差:

```
   结构化世界(我们写数据的方式)        一维世界(模型能吃的方式)
 ┌──────────────────────────┐        ┌─────────────────────────────┐
 │ [                        │        │ <bos>system\n你是助手<eos>   │
 │   {role:system, ...},    │  ===>  │ user\n你好<eos>             │
 │   {role:user,   ...},    │ 模板   │ assistant\n你好,有什么...   │
 │   {role:assistant,...}   │ 渲染   │ <eos>                       │
 │ ]                        │        │                             │
 └──────────────────────────┘        └─────────────────────────────┘
```

**chat template 就是这个 `===>` 箭头的具体规则。**

### 1.2 为什么必须和"训练时一致"

模型在预训练/微调时,见到的"对话长什么样"完全由模板决定。如果训练用模板 A、推理用模板 B(比如 role 标记不同、换行不同、特殊 token 不同),模型就会"看到自己从没见过的格式",效果断崖式下跌。

> 核心铁律:**推理时的 prompt 拼接方式,必须和训练时的渲染方式逐字符一致(包括空格、换行、特殊 token)。** 模板就是把这个"约定"固化下来、可复现的载体。

### 1.3 为什么是 Jinja

Jinja2 是 Python 生态里成熟的文本模板引擎(原本用于 Web 渲染 HTML)。它正好提供了对话渲染需要的三件事:

- **变量替换**:`{{ message['content'] }}` —— 把内容塞进文本。
- **循环**:`{% for message in messages %}` —— 一条条遍历对话轮次。
- **条件**:`{% if message['role'] == 'user' %}` —— 不同角色用不同包裹。

因为 Hugging Face Transformers 选择把 `chat_template` 字段(一段 Jinja 字符串)直接存进 `tokenizer_config.json`,Jinja 就成了开源社区事实上的"对话模板标准格式"。**模板随模型走、随 tokenizer 走**,这是它最大的价值。

## 2. 输入长什么样:messages 列表

SFT 数据集里一条样本,通常归一化成这样一个列表(这是业界通用约定,字段名以各框架文档为准):

```python
messages = [
    {"role": "system",    "content": "你是一个乐于助人的助手。"},
    {"role": "user",      "content": "用一句话解释什么是熵。"},
    {"role": "assistant", "content": "熵衡量一个系统的混乱/不确定程度。"},
]
```

- `role`:常见取值 `system` / `user` / `assistant`;带工具调用的还会有 `tool`(或 `function`)等。
- `content`:这一轮的文本(多模态场景可能是结构化片段,这里只谈纯文本)。

模板要做的,就是把这个列表"翻译"成模型能读的一长串。

## 3. 输出长什么样:渲染后的字符串

不同模型家族的模板风格不同,但思路一致:**每一轮用"角色标记 + 内容 + 结束标记"包起来,再首尾加全局标记**。下面是一个**示意性**(非任何具体模型逐字)的渲染结果:

```
<bos>
<|system|>
你是一个乐于助人的助手。<|end|>
<|user|>
用一句话解释什么是熵。<|end|>
<|assistant|>
熵衡量一个系统的混乱/不确定程度。<|end|>
```

关键观察:

- `<|system|>` `<|user|>` `<|assistant|>` 这类是**角色标记**(role marker),告诉模型"接下来这段话是谁说的"。
- `<|end|>`(或 `<eos>` / `</s>` 等)是**轮次/序列结束标记**,告诉模型"这一段说完了"。
- `<bos>` 是序列起始标记。具体用哪些 token、长什么样,**完全由该模型的 tokenizer 和模板决定,务必以官方为准**。

> 不同模型常见风格各异(有的用 `[INST]...[/INST]`,有的用 `<|im_start|>...<|im_end|>`,有的自定义)。**风格无所谓对错,关键是"训练与推理同一套"。**

## 4. 引擎内部:渲染流程

Jinja 渲染不是"字符串替换"那么简单,它是"编译 + 执行"。整体调用链:

```
  你的代码
     │  tokenizer.apply_chat_template(messages, ...)
     ▼
 ┌───────────────────────────────────────────────┐
 │ 1. 取出 tokenizer 里的 chat_template (Jinja 文本)│
 └───────────────────────────────────────────────┘
     │
     ▼
 ┌───────────────────────────────────────────────┐
 │ 2. Jinja 把模板"编译"成一个可执行的渲染函数      │
 │    (解析 {% %} {{ }},做语法树)                 │
 └───────────────────────────────────────────────┘
     │
     ▼
 ┌───────────────────────────────────────────────┐
 │ 3. 把 messages / 各种变量(如 add_generation   │
 │    _prompt、bos_token)作为"上下文"传进去执行    │
 └───────────────────────────────────────────────┘
     │
     ▼
 ┌───────────────────────────────────────────────┐
 │ 4. 输出一整串纯文本(prompt string)             │
 └───────────────────────────────────────────────┘
     │
     ├──(tokenize=False)──► 返回字符串,你自己看/再处理
     │
     └──(tokenize=True) ──► 继续交给 tokenizer.encode
                              得到 input_ids,喂给模型
```

要点:
- **第 1 步**说明模板"住"在 tokenizer 里。换模型 = 换模板,自动跟随。
- **第 2 步**的"编译"意味着语法错误会在这里暴露(比如 `{% for %}` 没闭合)。
- **第 3 步**的"上下文变量"是模板能力的来源:除了 `messages`,框架还会注入一些特殊变量(见第 6 节)。

## 5. 特殊 token、空白与"逐字符敏感"

这是最容易翻车的地方,单独讲清楚。

### 5.1 特殊 token 不是普通文字

`<bos>`、`<eos>`、`<|im_start|>` 这些在 tokenizer 词表里是**单个特殊 token**,而不是按字面拆成 `<`、`b`、`o`、`s`、`>` 几个普通字符。模板里写它们时,要确保它们最终被当成"特殊 token"对待(很多模板用 `{{ bos_token }}` 这种变量,而不是手写字面量,正是为了让它解析成正确的 token id)。

### 5.2 空白(空格 / 换行)是有意义的

模型对 `user\n` 和 `user ` 是两个不同 token 序列——它们 encode 出来不一样。所以模板里每一个 `\n`、每一个空格都是"协议的一部分"。

Jinja 有个细节会坑人:`{%- ... -%}` 里的 `-` 是**去空白控制符**,会吃掉标签前后的空白。模板作者用它来精确控制换行。你**改模板时动了这些 `-`,渲染结果就可能多/少一个换行**,从而和训练分布不一致。

```
 写法                  含义
 {% for x in xs %}     标签前后的换行会保留
 {%- for x in xs %}    吃掉标签"前面"的空白
 {% for x in xs -%}    吃掉标签"后面"的空白
```

> 调试口诀:**把渲染结果 `repr()` 出来看**(让 `\n` 和空格显形),不要只看肉眼打印,否则空白差异看不出来。

## 6. `apply_chat_template` 的关键参数(讲含义,不背默认值)

下面是几个最常用、最该理解的参数。**确切名字/默认值以 Transformers 官方文档为准**,这里讲"它解决什么、怎么权衡"。

| 参数(类别) | 作用 | 怎么权衡 |
|---|---|---|
| `tokenize`(布尔) | True 直接返回 input_ids;False 返回字符串 | 调试/想看渲染文本时用 False;直接喂模型用 True |
| `add_generation_prompt`(布尔) | **是否在末尾补上"该 assistant 说话了"的引导标记** | **推理时通常要 True**(否则模型不知道轮到它生成);**构造训练样本时通常 False**(因为答案已在 messages 里) |
| `return_tensors`(类别) | 返回 list / PyTorch / NumPy 张量 | 看下游框架 |
| `chat_template`(字符串/名称) | 临时指定一个模板,覆盖 tokenizer 自带的 | 一个 tokenizer 想支持多种格式(如普通对话 vs 工具调用)时用 |
| `tools` / 工具相关(类别) | 把可调用工具的描述注入模板 | function calling 场景才用,普通 SFT 不需要 |

最需要刻进脑子的是 **`add_generation_prompt`**:

```
 add_generation_prompt = False (训练/已含答案)
   ...<|assistant|>\n模型的回答<|end|>          ← 整段都在,用于算 loss

 add_generation_prompt = True (推理/等模型续写)
   ...<|user|>\n问题<|end|>\n<|assistant|>\n     ← 故意停在这里,等模型接着写
                                  ▲
                          引导标记已就位,光标在此
```

## 7. 训练用法 vs 推理用法

两种场景都用同一个模板,但目的不同:

```
┌───────────────────────── 训练(SFT) ─────────────────────────┐
│ 完整 messages(含 assistant 答案)                            │
│   ──模板渲染──► 完整字符串 ──► input_ids                      │
│ 通常 add_generation_prompt=False                             │
│ 还需要构造 labels:只对"assistant 该说的部分"算 loss,        │
│   prompt 部分(system/user)mask 掉(-100)                   │
└──────────────────────────────────────────────────────────────┘

┌───────────────────────── 推理(部署) ───────────────────────┐
│ messages 只到 user 最后一句(没有 assistant 答案)           │
│   ──模板渲染──► prompt 字符串(末尾带 generation prompt)     │
│ add_generation_prompt=True                                   │
│ 模型从这个 prompt 往后生成答案                                │
└──────────────────────────────────────────────────────────────┘
```

**"只对 assistant 部分算 loss"** 是 SFT 的重要细节:你不希望模型去"学习模仿 user 的提问",只想让它学"如何作答"。所以需要知道渲染后字符串中"哪一段是答案",据此做 label mask。有的模板/工具链能直接给出 assistant 区间的 mask(常称 `return_assistant_tokens_mask` 一类能力,**具体接口以官方为准**),否则就得自己按角色标记定位。

## 8. 自己写/调试一个最小模板

一个极简(教学用,非生产)Jinja 模板大致长这样:

```jinja
{{ bos_token }}
{%- for message in messages %}
{{ '<|' + message['role'] + '|>\n' }}
{{ message['content'] }}
{{ '<|end|>\n' }}
{%- endfor %}
{%- if add_generation_prompt %}
{{ '<|assistant|>\n' }}
{%- endif %}
```

逐句读:
- `{{ bos_token }}`:开头放序列起始 token。
- `{% for message in messages %}`:遍历每一轮。
- `'<|' + message['role'] + '|>\n'`:拼角色标记,比如 `<|user|>\n`。
- `message['content']`:塞入这一轮文本。
- `'<|end|>\n'`:这一轮结束标记。
- 最后 `if add_generation_prompt`:推理时补一个空的 `<|assistant|>\n` 让模型接着写。

### 调试三板斧

1. **`tokenize=False` 看渲染字符串**,再 `repr()` 让空白显形。
2. **和官方示例逐字符 diff**:确认 BOS/EOS、role 标记、换行完全一致。
3. **encode 后看 input_ids**:确认特殊 token 是单个 id,而不是被拆成普通字符。

## 配置示例:在 SFT 流水线里的位置

```
 原始对话数据 (jsonl, 每行一条 messages)
        │
        ▼
 ┌──────────────────────────┐
 │ 清洗 / 去重 / 质量过滤     │  ← 数据工程上游
 └──────────────────────────┘
        │  归一化成 messages 列表
        ▼
 ┌──────────────────────────┐
 │ apply_chat_template       │  ← 本文主角(Jinja 在这里执行)
 │  (+ add_generation_prompt)│
 └──────────────────────────┘
        │  渲染字符串 / input_ids
        ▼
 ┌──────────────────────────┐
 │ tokenize + 构造 labels     │  ← 对 prompt 部分做 -100 mask
 │  (+ padding / truncation) │
 └──────────────────────────┘
        │
        ▼
   DataLoader ──► 训练循环(算 loss、反传)
```

把握一个原则:**模板渲染发生在"数据已归一化为 messages"之后、"tokenize 成 ids"之前**,它是数据格式的"最后一道翻译"。

## 常见问题 / 坑

| 现象 / 坑 | 原因 | 应对 |
|---|---|---|
| 推理效果远差于训练评测 | 推理 prompt 拼法和训练模板不一致(多/少换行、role 标记不同) | 始终用同一个 `apply_chat_template`,别手写字符串拼 prompt |
| 模型不停不下来 / 不会换轮 | 缺少 EOS / 轮次结束标记,或模板没正确放结束 token | 检查渲染串里每轮是否有结束标记,且是真特殊 token |
| 推理时模型不"开口"作答 | 推理忘了 `add_generation_prompt=True` | 推理务必开;训练构样本时通常关 |
| BOS 被加了两次 | 模板里写了 `{{ bos_token }}`,encode 时又自动加了一次 | 明确谁负责加 BOS,二选一,别重复 |
| 特殊标记被拆成普通字符 | 模板里手写字面量,没作为特殊 token 注册 | 用 `{{ bos_token }}` 等变量,或确保 token 在词表中 |
| loss 异常 / 学会复读提问 | 没做 label mask,把 user/system 也算进 loss | 只对 assistant 区间算 loss,其余 mask 成 -100 |
| 改了模板渲染就乱了 | 动到了 `{%- -%}` 的去空白符,空白对齐被破坏 | 改前后做 `repr()` diff,逐字符核对 |
| 多轮对话格式对不上 | 循环里 turn 之间的换行/分隔处理不当 | 单独构造 1 轮、2 轮样本分别渲染验证 |

> 终极心法:**chat template 是一份"协议",训练方和推理方必须签同一份。** Jinja 只是把这份协议写成可执行、可随模型分发的文本而已。

## 🔗 跳转链接

- [[00-知识地图]] — 全局导航
- [[llm-train/README]] — SFT/微调训练总览,模板在训练数据准备阶段
- [[llm-alignment/RLHF]] — RLHF/对齐同样依赖对话模板拼接 prompt
- [[llm-algo/transformer/模型架构]] — 模型只吃一维 token 序列,理解模板存在的根因
- [[llm-application/README]] — 推理部署时如何正确拼 prompt
