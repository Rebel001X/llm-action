# 多 GPU 扩展效率模型 —— 依赖(全部为纯 CPU,无需 GPU/联网)
# 建议 Python >= 3.9;本项目在 Python 3.13 下开发验证。
numpy>=1.24
matplotlib>=3.6
pytest>=7.0

# 说明:
# - 本项目为"性能模型/容量规划"教学,不做真实分布式训练,故不依赖 torch。
# - torch 仅在 README 的"如何对接真实 DDP 测量"章节作为可选参考出现,非运行必需。
