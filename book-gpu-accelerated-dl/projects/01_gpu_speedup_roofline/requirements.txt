# ============================================================
# 01_gpu_speedup_roofline 依赖
# 本机已装，全部离线可用；无需 GPU / CUDA / 联网 / 权重下载。
# ============================================================
numpy>=1.24        # 大矩阵乘(GEMM)与逐元素运算的实测；核心公式实现
matplotlib>=3.7    # Roofline / 加速比 出图（Agg 后端，不弹窗）
pytest>=7.0        # 单元测试

# 可选：torch 仅用于「延伸」章节的对照实验，核心功能不依赖它。
# 本机为 torch 2.12 CPU 版；没有它也能跑通全部测试与 run_demo。
# torch>=2.0
